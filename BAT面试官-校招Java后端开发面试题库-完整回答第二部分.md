# BAT 面试官 - daijia 项目面试题库完整回答（第二部分）

> 续 Q33-Q104，所有回答均基于项目实现、口语化表达

---

## 六、消息队列与异步处理（续）

### 32. 如果消息消费失败了，应该如何处理？是重试还是丢弃还是进入死信队列？

好的。消息消费失败的处理策略很重要。首先不能直接丢弃，因为可能是临时故障，重试可能就成功了。一般的策略是重试。RabbitMQ 支持重试机制，消费失败后会自动重新投递消息。我们可以设置重试次数，比如最多重试三次。但是不能无限重试，因为可能是业务逻辑本身有问题，再怎么重试也不会成功。所以超过重试次数后，应该把消息放入死信队列。死信队列是一个特殊的队列，用来存放无法被正常消费的消息。然后人工去处理这些死信消息，分析问题原因。

### 33. 为什么需要消息确认机制？自动确认和手动确认有什么区别？

好的。消息确认机制是保证消息不丢失的关键。如果消费者接收了消息但没有处理完就宕机了，没有任何机制来记录这个消息，那消息就丢失了。消息确认机制就是用来解决这个问题的。自动确认是指消费者接收消息后立即自动确认，不管是否处理成功。缺点是如果处理失败了，消息还是被删除了，无法重试。手动确认是指消费者处理完消息后手动发送确认信号，RabbitMQ 才会删除消息。优点是更安全，如果处理失败了就不确认，消息会被重新投递。在我们的项目中，对于关键的消息比如支付成功，我们使用手动确认。

### 34. RabbitMQ 的三种交换机类型（Direct、Fanout、Topic）分别是什么？项目中用了哪种？

好的。RabbitMQ 有三种交换机类型。首先是 Direct 交换机，它根据完全匹配的路由键来转发消息。比如消息的路由键是 "order.paid"，交换机会把它发送到绑定了这个路由键的队列。其次是 Fanout 交换机，它忽略路由键，把消息发送到所有绑定了这个交换机的队列。最后是 Topic 交换机，它支持模糊匹配路由键。比如路由键可以是 "order.*" 或者 "*.paid"，这样可以匹配多个相关的消息。在我们的项目中，支付成功后发送消息到 RabbitMQ，我们使用的是 Topic 交换机。定义了一个路由键比如 "payment.success"，任务服务通过这个路由键来接收支付成功的消息。

---

## 七、分布式事务与一致性

### 35. 在微服务架构中，分布式事务是很大的问题。你了解 Seata 吗？它是怎么解决分布式事务的？

好的。分布式事务确实是微服务架构中的难题。Seata 是一个开源的分布式事务框架，可以保证多个服务的数据一致性。它主要有两种模式。第一种是 AT 模式，自动模式。应用不需要做任何改动，Seata 会自动拦截 SQL 语句，记录数据的前置和后置镜像，如果事务失败就自动回滚。第二种是 TCC 模式，需要应用自己实现 Try、Confirm、Cancel 三个方法。Try 是预留资源，Confirm 是提交，Cancel 是取消。Seata 会协调这三个方法的执行。在我们的项目中，支付成功后需要同时更新支付表和任务表，这是个跨服务事务。理论上应该用 Seata 来保证事务一致性，但目前我们项目中还没有启用 Seata。我们采用的是 RabbitMQ 异步处理加幂等性检查的方案来保证最终一致性。

### 36. 如果分布式事务失败了，系统应该如何处理？有什么策略吗？

好的。分布式事务失败会导致数据不一致，需要有相应的处理策略。首先是重试，Seata 会自动重试几次。其次是人工介入，如果自动重试还是失败了，需要人工分析原因并手动修复数据。再次是最终一致性策略，虽然暂时不一致，但通过定期的对账和修复，最终会保证数据一致。比如我们可以定期检查支付记录和任务状态是否一致，如果不一致就自动修复。

### 37. 你的项目中有没有使用全局事务？为什么支付流程需要全局事务？

好的。支付流程确实需要保证数据一致性。因为支付涉及金钱，任何数据不一致都会导致严重的后果。比如用户支付成功了，但任务状态没有更新，企业就不知道应该支付成功了。或者相反，任务状态更新成支付成功，但支付记录没有更新，就无法对账。所以支付流程必须保证原子性。在我们的项目中，虽然没有启用 Seata 的全局事务，但我们通过 RabbitMQ 异步处理加上幂等性检查来实现最终一致性。具体做法是支付成功后发消息到 MQ，任务服务消费时检查状态确保幂等。这样可以在没有强一致性的情况下保证数据最终一致。

---

## 八、任务调度与分配算法

### 38. dispatch 调度服务是怎么分配任务给无人机飞手的？采用了什么算法？

好的。dispatch 调度服务主要通过 XXL-Job 定时任务来实现。整个流程是这样的。首先，当企业创建一个新任务时，系统会创建一个定时调度任务，每分钟执行一次。其次，执行任务时，我们通过远程调用 LocationFeignClient 搜索附近的飞手。这里会根据任务的起点坐标和距离范围来搜索。第三步，搜索到满足条件的飞手列表后，我们使用 Redis 的 List 结构为每个飞手创建临时队列，将新订单信息推送到飞手的队列中。同时我们使用 Set 结构来防止重复推送给同一飞手。第四步，飞手通过 App 端看到新订单，自己决定是否接单。如果接单，任务状态更新，同时停止定时调度。整个过程是比较简洁的设计，主要依赖地理位置搜索和飞手的主动选择，而不是算法主动分配。这在实际应用中可以进一步优化，比如加入更多的分配策略。

### 39. 如果同时有很多任务需要分配，怎么保证不会出现重复分配？

好的。重复分配会导致多个飞手接同一个任务，这是严重的问题。理想的解决方案是使用分布式锁。当某个飞手接了任务后，我们应该在 Redis 中设置一个分布式锁，键是任务 ID，值是飞手 ID。后续的分配逻辑在分配前先检查这个锁，如果锁已经存在就说明任务已经被分配了，就不再分配给其他飞手。使用 Redis 的 SETNX 命令可以保证原子性。不过老实说，项目中这块的实现还不够完善，这是后续可以加强的地方。

### 40. 如果分配给飞手的任务，飞手拒绝了怎么办？

好的。这是很常见的情况。比如飞手的无人机电量不足或天气条件不适合飞行。如果飞手拒绝了任务，我们需要立即清除这个分布式锁，然后重新触发分配逻辑，去找其他合适的飞手。同时我们会记录飞手的拒单行为，这对后续的评分和优先级有影响。如果一个飞手经常拒单，我们会降低他在分配中的优先级，优先分配给更可靠的飞手。

### 41. 任务分配的时候，怎么避免分配给已经超载的飞手？

好的。飞手同时接太多任务会导致服务质量下降，也可能因为飞行时间过长导致无人机电量不足。所以我们在分配时会检查飞手的当前工作量和无人机状态。比如我们可以定义每个飞手最多同时执行 3 个任务，或者根据飞行距离估算时间，确保任务能在无人机电量范围内完成。在分配时先查询飞手当前执行的任务数和剩余电量，如果已经达到上限或电量不足就不再分配给他。这样可以保证每个飞手的工作量均衡，也保证了飞行安全。

---

## 九、并发控制

### 42. 如果很多用户同时下单创建任务，系统怎么处理？会不会出现问题？

好的。高并发是分布式系统的一个大挑战。如果很多用户同时下单，每个请求都会创建一个任务，然后去分配飞手。首先从数据库的角度，我们需要确保写入不会冲突。MyBatis-Plus 自动处理了这个，每个插入操作都有一个唯一的任务 ID。其次从业务的角度，我们需要确保资源分配不会冲突。比如一个飞手不能同时接多个任务。我们通过分布式锁来解决这个问题。再次从系统架构的角度，我们可以通过扩展服务的实例来提高并发处理能力。比如部署多个 task 服务实例，通过负载均衡器分散请求。

### 43. 什么是分布式锁？为什么需要分布式锁？

好的。分布式锁是在分布式系统中协调多个进程对共享资源的访问的机制。在单机系统中，我们可以用 synchronized 或 ReentrantLock 来保护临界区。但在分布式系统中，多个服务可能运行在不同的机器上，JVM 级别的锁就不起作用了。比如任务分配时，两个调度实例同时为同一个飞手分配任务，单机锁没法保护。这时候就需要分布式锁。分布式锁一般通过 Redis、Zookeeper 等共享存储来实现。

### 44. 如何用 Redis 实现分布式锁？注意哪些坑？

好的。Redis 实现分布式锁的基本思路是用 SET 命令的 NX 选项，只有当 key 不存在时才设置。比如要给某个资源加锁，可以执行 SET resource_key lock_value NX EX timeout。NX 保证原子性，EX 设置过期时间避免死锁。如果返回 OK 就加锁成功，否则加锁失败。释放锁时用 DEL 命令删除这个 key。注意的坑有几个。第一是要设置过期时间，否则如果持有锁的进程崩溃了，锁永远不会释放，其他进程就永远等待。第二是释放锁时要检查 key 的值是否是自己设置的，避免释放别人的锁。第三是锁的过期时间要设置得合理，既不能太短导致任务还没完成锁就过期了，也不能太长。这些都是理论上应该做的，具体项目实现中可能还有改进空间。

### 45. 为什么需要乐观锁？什么时候应该用乐观锁而不是悲观锁？

好的。乐观锁和悲观锁是两种不同的并发控制策略。悲观锁假设冲突会发生，所以先加锁再操作，比如 SELECT FOR UPDATE。乐观锁假设冲突不会发生，直接操作，然后检查是否有冲突。实现乐观锁一般通过版本号，比如在表中加一个 version 字段。更新时检查版本号是否改变，如果改变了说明有冲突就回滚。乐观锁适用于读多写少的场景，因为大多数操作不会冲突，避免加锁的开销。悲观锁适用于写多的场景，因为冲突经常发生，加锁可以避免重复失败。

---

## 十、日志与监控

### 46. 在微服务系统中，怎么追踪一个请求在各个服务间的调用链路？

好的。这是个很重要的运维问题。在单机系统中可以用一个线程的日志来追踪。但在微服务系统中，一个请求可能跨越多个服务和多个服务器，用线程 ID 就不行了。解决方案是使用分布式链路追踪。我们可以在请求进入系统时生成一个全局唯一的 trace ID，然后在服务间传递这个 ID。比如支付服务调用任务服务时，在 HTTP 请求头中加上 trace ID，任务服务在响应时也带上这个 ID。这样所有涉及这个请求的日志都会记录这个 trace ID，就可以追踪完整的调用链路。

### 47. 怎么收集各个服务的日志？是否需要集中式日志管理？

好的。在微服务系统中，每个服务都会产生日志。如果各自管理日志会很混乱，而且不利于问题排查。最好的方案是集中式日志管理。比如用 ELK Stack（Elasticsearch、Logstash、Kibana）。所有服务的日志都发送到 Logstash，然后聚合到 Elasticsearch，通过 Kibana 展示和搜索。这样可以统一查看所有服务的日志，还能搜索和分析。这是我认为应该做的一个优化方向，目前项目中还没有完全落地。

### 48. 什么是告警和监控？为什么微服务系统需要监控？

好的。监控是指对系统运行状态的实时观察和记录，比如 CPU 使用率、内存使用率、服务可用性等。告警是指当监控数据超过阈值时，系统自动发出警告。为什么需要监控呢，因为微服务系统很复杂，各个服务之间有依赖关系。如果某个服务出现问题，可能会导致连锁反应。通过监控可以及时发现问题，在问题扩大前进行处理。比如如果任务服务的响应时间突然变慢，监控会发出告警，运维人员可以立即排查原因。

### 49. 你的项目中有哪些关键指标需要监控？

好的。对于我们的项目，有几个关键指标需要监控。首先是服务可用性，要监控每个服务的在线状态和错误率。其次是性能指标，比如 API 响应时间、数据库查询时间等。如果某个服务的平均响应时间超过 1 秒，就需要告警。再次是业务指标，比如任务创建数、任务完成数、支付成功率等。如果支付成功率突然下降，说明可能出现了问题。第四是资源使用情况，比如 CPU、内存、磁盘空间等。如果 Redis 内存使用率超过 80%，就需要告警。这些监控体系还在逐步完善中。

---

## 十一、安全性

### 50. 在支付流程中，怎么保护用户的敏感信息？

好的。支付涉及用户的敏感信息，比如支付账户、支付密码等，必须妥善保护。首先是数据不能明文存储，比如用户密码不能直接存在数据库，要用 MD5 或更强的算法加密。其次是数据传输必须使用 HTTPS，确保数据在网络传输中不被窃听。再次是对敏感数据要进行脱敏，比如在日志中不能记录完整的支付账户，可以只记录后四位。第四是限制数据访问权限，只有必要的人员和系统才能访问支付数据。第五是定期审计，记录谁访问了哪些数据，便于追踪和排查。

### 51. 怎么防止支付的重复扣款？

好的。重复扣款是支付系统中的一个严重问题。首先是通过幂等性来防止，前面提到过，要确保支付操作是幂等的。其次是使用分布式锁，当处理支付时加锁，确保不会有两个请求同时处理同一个订单。再次是记录支付状态，支付记录表中要明确记录支付状态，比如待支付、已支付、支付失败等。处理支付前先检查状态，如果已经支付过就不再处理。第四是定期对账，定期检查支付记录和任务状态是否一致，如果不一致就自动修复。

### 52. 怎么防止 SQL 注入？

好的。SQL 注入是一种常见的网络攻击，攻击者通过在输入中插入恶意 SQL 代码来执行非法操作。防止 SQL 注入的方法有几个。首先是使用参数化查询，也叫预编译语句。比如用 MyBatis 的 #{}，它会把参数当作一个值而不是 SQL 代码，自动进行转义。永远不要用字符串拼接来构建 SQL 语句。其次是对用户输入进行验证和过滤，只允许预期的格式。再次是使用 ORM 框架，比如 MyBatis-Plus，它会自动处理参数转义。第四是对数据库账户进行最小权限原则，使用只有必要权限的数据库用户。

### 53. API 接口如何进行身份验证和授权？

好的。身份验证是确认用户是谁，授权是确认用户能做什么。对于 API 接口，常见的身份验证方法有几个。首先是用户名和密码，但这种方法在每个请求中都要传递密码，不安全。其次是 Token 方式，用户登录后系统返回一个 Token，后续请求在 HTTP 头中携带这个 Token。系统验证 Token 的有效性来确认用户身份。再次是 JWT（JSON Web Token），它是自包含的，不需要服务器查询就能验证。对于授权，我们通常使用角色和权限的概念。比如企业用户可以创建和查询任务，飞手用户可以查看分配给他们的任务。在 Spring Security 中可以定义不同的角色和权限，通过 @PreAuthorize 注解来控制接口访问。

### 54. 你的项目中怎么处理敏感数据的日志记录？

好的。在开发中经常需要记录日志来调试和分析问题，但日志中不能包含敏感数据。比如支付金额、用户账户信息等。处理方法有几个。首先是对敏感字段进行脱敏，比如支付账户只记录后四位，中间的数字用 * 代替。其次是在日志配置中排除敏感字段，比如配置哪些字段不应该被记录。再次是对日志文件进行访问控制，只有授权的人员才能查看日志。第四是使用专门的加密日志系统，敏感信息在存储时就被加密，只有在需要查看时才解密。

---

## 十二、系统性能与优化

### 55. 你的项目中哪些地方容易成为性能瓶颈？

好的。常见的性能瓶颈有几个地方。首先是数据库查询，如果查询没有使用合适的索引，或者一次性查询大量数据，就会很慢。其次是网络 I/O，微服务间的远程调用涉及网络传输，比本地调用慢得多。如果一个接口需要调用多个其他服务，网络延迟会累加。再次是缓存穿透或缓存失效，大量请求打到数据库导致数据库压力大。第四是并发控制不当，如果使用悲观锁保护高并发场景，会导致大量请求排队等待。第五是算法复杂度，比如任务分配算法如果时间复杂度太高，会消耗大量 CPU。

### 56. 怎么优化数据库查询性能？

好的。优化数据库查询性能的方法有几个。首先是创建合适的索引，对于频繁查询的字段和 WHERE、ORDER BY、JOIN 条件中的字段要建立索引。其次是避免 SELECT *，只查询需要的字段，减少数据传输量。再次是使用分页查询，避免一次性加载大量数据到内存。第四是避免复杂的 JOIN，如果一个查询需要 JOIN 多个表，可以考虑拆分成多个查询。第五是使用查询缓存或者 Redis 缓存，对于读多写少的数据可以缓存。第六是定期分析慢查询，数据库可以记录执行时间超过阈值的查询，我们可以针对这些慢查询进行优化。

### 57. 怎么减少微服务间的网络调用？

好的。微服务间的网络调用是一个主要的性能瓶颈，怎么减少呢。首先是使用本地缓存，把常用的数据缓存在本地，避免频繁的远程调用。其次是合并请求，如果需要调用多个服务获取不同的数据，看能不能一次调用获取所有数据。再次是使用异步调用，对于不需要立即返回结果的操作，可以发消息到 MQ 异步处理，不用等待。第四是使用 API Gateway 做接口聚合，把多个服务的数据聚合后返回给前端，减少前端的请求次数。第五是定期审查服务间的调用关系，如果发现某个服务总是调用另一个服务，考虑把逻辑合并或者搬到同一个服务。

### 58. 压测是什么？为什么需要压测？

好的。压测是指对系统进行高并发、大流量的模拟测试，看系统能否正常运行以及能承受多大的压力。为什么需要压测呢，因为系统在开发时通常只有几个人测试，流量很小。但上线后可能会有大量用户同时访问，如果系统没有经过压测，就不知道能不能承载这么大的流量。通过压测可以提前发现瓶颈和问题，比如发现某个接口在 1000 并发时就挂了，可以在上线前进行优化。常见的压测工具有 Apache JMeter、Locust 等。

### 59. 怎么设计一个高可用的系统架构？

好的。高可用是指系统能够长时间正常运行，即使部分组件故障也不会导致整个系统不可用。设计高可用架构的几个原则。首先是冗余，关键组件要部署多个实例，这样一个实例故障了还有其他实例。比如部署多个 task 服务实例，前面用 load balancer 分散请求。其次是故障隔离，要用断路器、熔断器等技术来隔离故障，防止故障扩散。再次是快速故障检测和转移，要定期检查服务健康状态，如果某个实例故障了要立即将流量转移到其他实例。第四是数据备份和恢复，数据库要有主从模式或者其他备份机制，数据丢失了可以恢复。第五是灰度发布，新版本上线时先发布到一部分实例，观察是否有问题，没问题再逐步扩展。

---

## 十三、开发经验与反思

### 60. 在开发这个项目过程中，遇到过最大的挑战是什么？怎么解决的？

好的。在开发过程中最大的挑战是分布式系统的数据一致性。因为任务、支付、企业等数据分散在不同的服务和数据库中，很难保证数据的一致性。比如支付成功了但任务状态没有更新，或者任务被分配给了两个飞手。刚开始的时候我们用同步调用，但这样会导致服务间的强依赖，一个服务故障就会导致整个流程失败。后来我们改成异步处理加上幂等性检查，通过 RabbitMQ 发送消息，任务服务消费消息进行处理。这样可以解耦服务之间的依赖，即使任务服务暂时不可用，消息也会等待，等服务恢复后继续处理。另外我们还定期进行对账，检查各个服务的数据是否一致，如果不一致就自动修复。通过这些措施基本解决了数据一致性的问题。

### 61. 项目中有没有使用过一些设计模式？

好的。我们在项目中应用了一些设计思想。首先是关于分层和职责分离，每个 service 类只负责一个领域的业务逻辑，不同的 Controller 对应不同的业务。其次是关于 Feign 客户端的使用，把远程调用抽象成接口，符合依赖倒置原则。再次，在支付处理中，我们使用了幂等性检查的思想，避免重复操作。第四是在 RabbitMQ 的使用中，体现了异步解耦的设计思想。这些都是实践中自然应用的设计原则，具体的设计模式实现还可以进一步优化。

### 62. 你觉得代码
