# daijia项目 - 简历版本面试题库完整回答

> 50个面试题的简洁口语化回答，可直接口述

---

## 一、架构设计相关（Q1-Q10）

**Q1: 为什么选择Spring Cloud微服务架构而不是单体架构？**

系统涉及任务、支付、调度、用户等多个功能模块，放在单体项目里会很臃肿，代码耦合度高，后期维护和扩展都困难。用微服务的话，每个模块可以独立开发、部署、扩展，更灵活。而且无人机平台业务逻辑复杂，不同模块协作频繁，微服务能很好地处理这种场景。

**Q2: Nacos在系统中的具体作用是什么？和Eureka对比有什么优势？**

Nacos既是服务注册中心又是配置中心。每个微服务启动时向Nacos注册自己，从Nacos拉取配置信息。相比Eureka只能做注册中心，Nacos更全面。我们系统的支付密钥、分配参数这些都通过Nacos动态配置，改了配置不用重启服务就能生效，特别方便。

**Q3: 9个微服务的职责分工如何划分？为什么这样拆分？**

任务服务负责任务生命周期管理，调度服务负责任务分配，支付服务处理微信支付，企业和飞手服务分别管理两类用户，Map服务提供位置查询，MQ服务管理消息队列，网关服务做流量入口，公共服务处理日志监控。按功能域拆分，每个团队只需关注自己的模块，不用理解整个系统。

**Q4: 微服务之间如何通信？同步和异步分别用在什么场景？**

同步调用用Feign，这是RestTemplate的封装，用起来像调本地方法一样简单。比如任务服务调用飞手服务查飞手信息就用Feign。异步的话用RabbitMQ，比如支付成功后通知任务服务这种不需要马上返回结果的操作就异步处理。异步的好处是系统响应快，服务之间解耦。

**Q5: Feign和直接HTTP调用相比有什么优势？**

Feign集成了Ribbon的负载均衡，会自动从Nacos获取服务地址列表，用轮询算法分散请求到不同实例。我们不用手动管理IP地址，有实例挂了Feign会自动转移请求到其他实例。另外Feign用起来特别简洁，直接写接口就行，会自动生成HTTP逻辑。

**Q6: RabbitMQ在系统中用来处理哪些业务？为什么要用消息队列？**

主要是支付成功后异步通知任务服务、定期对账这类后台任务。用消息队列的好处是支付服务不用等待任务服务处理完，支付完了立即返回，响应时间就降下来了。而且如果任务服务宕机，消息会缓存在RabbitMQ里，服务恢复后继续处理，不会丢失数据。

**Q7: 网关服务具体实现了哪些功能？限流是怎么做的？**

网关是所有API请求的入口，主要职责是路由、限流、鉴权。限流用令牌桶算法，配置比如每秒最多允许1000个请求。超过限流阈值的请求会被拒绝，网关返回429错误码。这样可以防止某个服务被打崩，保护后端服务稳定性。

**Q8: 服务注册中心（Nacos）宕机了，系统怎么办？**

Nacos宕机有两种影响：第一，新启动的服务无法注册，但已启动的服务仍然可以继续工作。第二，Feign客户端无法动态更新服务列表，但它们会缓存已知的服务地址，仍然可以调用。所以短期内不会对系统有太大影响，长期的话就需要尽快恢复Nacos。

**Q9: 如何保证微服务之间的通信可靠性？**

对于同步调用用Feign，可以配置重试次数和超时时间，如果某个实例响应超时会自动转移到其他实例。对于异步调用用RabbitMQ，我们配置了消息持久化、手动确认机制，确保消息不丢失。另外还有定期对账逻辑，万一有消息没被正确处理也能发现和补偿。

**Q10: 系统有多少个数据库？数据库之间的关系是什么？**

现在是单个数据库，但逻辑上分为几个模块的表：任务模块有任务表、任务状态日志表等，支付模块有支付表、订单表，用户模块有飞手表、企业表等。如果数据量增长到百万级，我们会考虑分库分表，比如按飞手ID分表或按时间分库。

---

## 二、任务分配模块（Q11-Q22）

**Q11: 任务分配的完整流程是怎样的？为什么采用这个方案？**

新订单来的时候，系统会根据订单位置在Redis里搜索5公里范围内的所有飞手。然后把订单信息推送给这些飞手，飞手看到后可以选择接或不接。这样设计的好处是简单高效，飞手有自主权，不会被迫接不想要的任务。一开始我们想用复杂的加权算法，后来发现性能跟不上，改成这个方案后系统稳定多了。

**Q12: 为什么不用复杂的多因素加权算法，反而用简单的地理位置推送？**

复杂算法的问题是参数难以调优，不同因素的权重怎么确定没有明确标准。而且在高并发的时候性能成了瓶颈，计算每个飞手的匹配分数需要时间。反而简单的地理位置推送加广播模式，虽然不一定是最优分配，但胜在高效稳定。而且飞手自己选择接单，从用户体验角度也更好。

**Q13: XXL-Job定时任务每次执行间隔多长？为什么要定时重复分配？**

执行间隔大概是1分钟。如果第一轮推送没有飞手接单，系统需要继续尝试，定时任务就会再次执行分配逻辑。这样能确保任务最终不会被遗漏，即使最近的飞手都很忙，下一轮也许会有闲着的飞手。重复分配直到任务被接单或超时自动取消。

**Q14: 地理位置搜索的范围是多少？5公里这个数字怎么确定的？**

5公里是根据无人机的续航能力和实际园区大小估算的。无人机一般续航能力是30到50分钟，在园区里平均速度大概是每小时50公里，所以往返飞行的话5公里范围内来回还能有时间余量。这个数字不是绝对的，后续可以根据实际运营情况调整。

**Q15: Redis的Set结构如何防止同一订单重复推送给同一飞手？**

对每个订单创建一个Set类型的Redis key，比如"ORDER_123_DRIVERS"。遍历要推送的飞手列表时，先检查这个飞手ID是否在Set里。如果没有就把他加进去，然后推送订单。如果已经在里面了就跳过。这个Set的过期时间设置为16分钟，过了就自动清理。

**Q16: Redis的List结构用来做什么？为什么选择List而不是其他数据结构？**

我们用List给每个飞手创建临时订单队列。飞手来APP查看待执行任务时，系统从他的队列里取订单信息。用List的好处是操作快，leftPush和rightPop都是O(1)时间复杂度。而且List天然有序，先推送的订单先被飞手看到。队列的过期时间设置为1分钟，防止积压太多旧订单。

**Q17: 飞手队列的过期时间为什么设置为1分钟？**

1分钟是基于业务逻辑的。订单推送给飞手后，希望飞手能快速响应。如果1分钟还没接单，说明飞手可能不感兴趣或没看到，系统就会在下一轮分配时重新推送。这样能及时把任务转移给其他飞手。太长的话任务等待时间长，太短的话容易导致任务重复推送。

**Q18: 1000+并发是如何实现的？系统的瓶颈在哪里？**

1000+并发主要是靠Redis的高性能。查询附近飞手、防重复推送这些操作都在Redis里完成，内存访问速度远快于数据库。另外用消息队列异步处理也提高了吞吐量。系统的瓶颈最终还是会落在数据库，如果并发再增长会考虑分库分表。

**Q19: 95%的任务分配准确率是怎么定义的？剩下5%是什么情况？**

准确率是指系统推送的任务被飞手正常执行的比例。剩下5%主要是天气恶劣无人机无法飞行、设备故障、飞手因为某些原因中途放弃等情况。还有极少数是算法本身的限制，位置搜索范围内可能没有合适的飞手。

**Q20: 没有飞手接单的任务怎么处理？有降级方案吗？**

如果一轮推送没人接，定时任务会在下一分钟继续尝试，可能扩大搜索范围或调整推送策略。多次重试失败后，任务会进入待处理队列，等待手工介入或继续自动匹配。没有真正的降级方案，因为任务分配是核心业务不能降级。

**Q21: 同一个飞手收到过多任务推送会怎样？有限制吗？**

目前没有硬性限制，但我们会监控飞手接单率。如果某个飞手收到很多推送但接单率很低，系统会自动降低他的优先级。另外飞手的APP里有一个待接订单的上限，比如最多显示20个待选任务，超过了就不再推送新的，防止飞手被淹没。

**Q22: 如何避免任务分配的不公平？如何评估分配质量？**

分配质量主要看两个指标：任务接单率和飞手工作量均衡度。我们会定期分析哪些飞手频繁被推送但不接单，说明他们对这类任务不感兴趣。另外会看飞手的工作饱和度，不能总是给某个飞手推送，要让所有飞手都有活干。这些数据都会上报到监控系统。

---

## 三、支付结算模块（Q23-Q36）

**Q23: 微信支付的完整流程是什么？从前端到后端的全链路。**

用户在APP点击支付，前端调用支付服务的接口，支付服务生成预支付订单并调用微信支付API，微信返回二维码或支付链接。用户扫码或点击链接进行支付，支付完成后微信调用我们的回调接口。我们验证微信的签名确认支付真实有效，然后更新支付记录、发送消息给任务服务处理后续业务。

**Q24: 微信回调接口的签名怎么验证？为什么一定要验证？**

微信会在回调中附上一个签名，这个签名是用微信的密钥对回调数据生成的。我们用同样的密钥和数据重新生成签名，如果和回调里的签名一致就说明回调是真实有效的。一定要验证是因为如果不验证，任何人都可以伪造一个回调请求来欺骗我们系统说用户已支付，这样就会被攻击。

**Q25: 为什么要用异步消息而不是支付后直接调用任务服务？**

直接调用有个问题：如果任务服务宕机了，整个支付流程就会失败，用户支付都没成功。用异步消息的话，支付成功立即返回给用户，消息缓存在RabbitMQ里。任务服务恢复后继续处理消息，不会丢失任何数据。这样系统的可用性和响应时间都提升了。

**Q26: 什么是幂等消费？为什么支付系统特别需要幂等性？**

幂等消费就是说同一条消息被消费多次的结果和消费一次的结果是一样的。支付系统特别需要这个是因为支付涉及到钱，出不得错。比如支付成功后我们要更新订单状态，如果没有幂等性保证，消息被重复处理就会重复更新，用户可能被重复扣款。有了幂等性，消息处理多少次结果都一样，用户只扣一次钱。

**Q27: 幂等消费是怎么实现的？具体代码逻辑是什么？**

我们的做法是通过检查数据库状态来实现的。消息到达的时候先查询订单的支付状态。如果已经是"已支付"状态，说明这个消息以前处理过了，就直接返回，不再处理。如果还是"未支付"状态，就把它改成"已支付"，然后继续执行后续的业务逻辑。这样即使同一个消息被消费多次，也只有第一次会真正改变数据，后续都是幂等的。

**Q28: 消息队列消息丢失怎么办？RabbitMQ怎么保证消息不丢失？**

我们的保障措施有几层。首先，RabbitMQ配置了消息持久化，消息会被持久化到磁盘，即使服务重启也不会丢。其次，消费端配置了手动确认机制，消息只有被正确处理后才会从队列里移除。如果处理失败或者没收到确认，消息会重新投递。最后，我们还有定期对账逻辑，如果发现有支付记录但任务状态没更新，就重新发送消息。

**Q29: 定期对账是什么时候做的？怎么处理账和业务不一致的情况？**

定期对账一般是每天晚上跑，我们从支付表里查所有已支付的订单，然后和任务表里的已执行任务进行对比。如果发现有差异，比如某个订单已支付但任务还是待支付状态，系统会自动补偿，重新发送消息给任务服务。对账发现的问题会上报告警，如果无法自动修复就需要人工介入。

**Q30: 支付成功后为什么还要再查一遍数据库判断状态？**

这是为了实现幂等性。消息可能被消费多次，如果每次都直接执行业务逻辑，就会产生多次扣款。查询一遍数据库看状态是否已经改过了，如果改过了就不再执行，这样就保证了幂等性。虽然查询会多一次数据库操作，但相比数据错误来说代价微不足道。

**Q31: 消息被重复投递多次怎么办？如何处理重复消费？**

这就是幂等消费要解决的问题。我们在消费逻辑里加了状态检查，每次消费前先检查订单是否已经被处理过。如果处理过就直接返回，不会执行重复操作。另外我们还用消息的唯一ID来追踪消息，防止重复投递。

**Q32: 支付成功率99.9%，失败的0.1%是什么原因？**

这个成功率主要是在压力测试中测出来的，实际生产环境未必能达到。失败的原因主要是网络超时、微信服务暂时不可用、我们的服务故障等。还有极少数是用户的问题，比如账户余额不足。我们通过重试、降级、限流这些机制来尽量提高成功率。

**Q33: 如果支付服务宕机，已经扣款的钱怎么办？**

支付服务宕机只会影响新的支付请求无法进行，已经扣款的交易不会受影响。因为微信支付是独立的系统，一旦扣款成功微信那边就有记录了。我们这边挂了也不影响。只要我们的回调接口能恢复，就能继续处理微信推送的回调，最终还是会和微信的记录保持一致。

**Q34: 支付和任务状态不一致（账不符）怎么对账和补偿？**

对账流程是：查支付表找出所有已支付的订单，查任务表看这些订单的状态。如果发现任务还是待支付或者没有对应记录，就认为是不一致。补偿方式是重新发送消息到MQ，让任务服务重新处理。也可以直接更新任务表，把状态改为已支付。如果补偿失败就上报告警，由人工处理。

**Q35: 用户投诉说支付成功了但任务没创建，怎么查证和处理？**

首先查支付表看这个用户的支付记录，确认支付确实成功了。然后查任务表看任务状态，如果没有任务记录就说明消息没被处理。可能是消息丢失或者任务服务当时宕机了。我们会主动重新发送消息，或者直接插入一条任务记录。同时通知用户处理已完成，不用再支付。

**Q36: RabbitMQ的手动确认机制是怎样的？不确认会怎样？**

手动确认就是消息被消费后不会自动从队列删除，只有我们主动调用确认函数才会删除。如果消费过程中出错，我们就不确认，这样消息会一直留在队列里。RabbitMQ会在一定时间后重新投递这条消息给其他消费者，这样就能保证消息不丢失。不手动确认的坏处是消息可能堆积，但好处是可靠性高。

---

## 四、并发控制与性能优化（Q37-Q50）

**Q37: 高并发下多个飞手同时接单，怎么保证一个任务只分配一个人？**

用分布式锁。当飞手点击接单按钮时，系统会尝试在Redis里对这个订单进行加锁。Redis的SET命令加NX参数，只有key不存在时才会设置成功。多个飞手同时接单时，只有第一个人的加锁会成功，其他人会失败，系统就告诉他们订单已被抢走。这样就保证了一个订单只分配给一个人。

**Q38: 分布式锁用Redis实现，为什么不用MySQL或其他方案？**

Redis是内存操作，性能特别快，适合高并发的场景。MySQL可以用但比较慢，并发多了容易造成数据库压力。另外Redis的SET命令本身就是原子的，不需要额外的同步机制，直接就能保证一致性。Zookeeper也可以，但它的部署和维护比较复杂，Redis相对更轻量。

**Q39: Redis分布式锁的过期时间为什么设置为10秒？**

10秒是基于飞手接单的平均时间估算的。接单的过程其实很快，从点击按钮到确认接受一般就几秒钟。10秒足够应付大多数情况。如果网络慢或系统卡可能会超时，但这是少数情况。设得太长的话如果飞手网络断掉锁就一直被占着，系统就死掉了。

**Q40: 10秒过期时间会不会太短？如果接单过程超过10秒怎么办？**

是有这个风险的。如果接单过程确实超过10秒，锁就会自动释放，其他飞手也能获得这个订单，这样就会出现一个订单被两个飞手同时接走的情况。为了应对这个问题，我们在后面的数据库操作时加了唯一性约束，也就是说插入任务时用飞手ID和订单ID的组合作为唯一键，最多只能插入一条。这样数据库会拒绝第二个飞手的插入，保证数据一致。

**Q41: 如何实现分布式锁的"看门狗"机制（自动续期）？**

看门狗机制就是获得锁之后，每隔一段时间（比如3秒）自动延长一下锁的过期时间。这样即使接单过程比较长，锁也不会过期。实现方法是开一个后台线程，定时执行EXPIRE命令重新设置过期时间。但我们现在还没实现这个机制，后续可以加上来提高可靠性。

**Q42: 分布式锁释放的时候需要验证持有者吗？为什么？**

需要的。比如说A飞手获得了锁，正在接单。10秒过期了，B飞手也获得了锁，开始接单。这时A飞手的接单过程突然完成了，他去删除锁，结果把B飞手的锁删掉了。这样就出问题了。所以删除锁的时候应该验证一下这个锁是不是自己设置的。具体方法是lock_value设置成一个唯一ID，删除时验证value是否匹配，只有匹配才删除。

**Q43: 三层缓存架构分别是什么？各层的过期时间是多少？**

第一层是本地缓存，用Caffeine库，存放热点数据比如飞手信息、系统配置，过期时间大概10分钟。第二层是Redis缓存，存放任务详情、用户信息，过期时间1小时。第三层是数据库。请求先查本地缓存，没有就查Redis，都没有就查数据库然后逐层回写。

**Q44: 本地缓存（Caffeine）和Redis缓存怎么协调？数据不一致怎么办？**

两层缓存确实容易不一致。我们的策略是更新数据时，先更新数据库，再删除Redis缓存，但不好删除本地缓存。所以本地缓存设置比较短的过期时间，比如10分钟，这样即使不一致也是暂时的。对大多数业务来说不一致几分钟是可以接受的，反正最终还是会一致。

**Q45: 缓存更新策略是什么？Cache-Aside还是Write-Through？**

我们用的是Cache-Aside。读数据时查缓存，没有就查数据库然后写入缓存。写数据时先更新数据库，再删除缓存。这个策略比较简单通用，但有个缺点就是可能出现缓存穿透，也就是大量查询一个不存在的数据，每次都要查数据库。我们通过布隆过滤器来解决，对不存在的数据标记一下，下次直接返回没有。

**Q46: 如何识别热点数据？哪些数据放在本地缓存里？**

热点数据我们主要通过监控日志来识别，看哪些数据被查询最频繁。一般来说飞手的基本信息、高频被访问的任务、系统参数这些都是热点。本地缓存的容量有限，一般放在百万级以内的小数据，而且是改动不频繁但访问特别频繁的数据。大数据或者频繁改变的数据不适合放本地缓存。

**Q47: API响应时间从500ms优化到200ms，具体优化了哪些点？**

主要优化了四个方面。第一是加缓存，避免频繁查数据库。第二是数据库优化，加索引、避免复杂的多表join。第三是异步处理，把一些耗时操作放到后台任务或消息队列里。第四是减少网络开销，避免微服务间不必要的调用，通过本地缓存或批量操作来减少RPC调用次数。

**Q48: 数据库有建立哪些索引？怎么决定在哪些字段建索引？**

任务表在user_id、status、created_time上建了索引，支付表在order_id和payment_status上建了索引。还有复合索引(user_id, status)，用来快速查询用户的待处理任务。索引选择的原则是看哪些字段经常出现在where和order by子句里，这些字段建索引能显著提升查询速度。

**Q49: 复合索引(user_id, status)的作用是什么？顺序能换吗？**

这个复合索引的作用是支持"查询某个用户的所有待处理任务"这类查询。顺序最好不要换，因为在B+树索引里，第一个字段是主排序字段，第二个字段是辅助排序。如果换成(status, user_id)就没办法快速查询某个用户的待处理任务了。

**Q50: 缓存穿透、缓存击穿、缓存雪崩分别怎么解决？**

缓存穿透是大量查询一个不存在的数据，每次都要查数据库。解决办法是用布隆过滤器标记不存在的数据。缓存击穿是某个热点数据的缓存失效了，大量请求同时查数据库。解决办法是用分布式锁保护，第一个请求去查数据库并更新缓存，其他请求等待。缓存雪崩是大量缓存同时失效，系统一下子被打崩。解决办法是错开缓存失效时间，不要让所有缓存都在同一时刻过期，或者加热点缓存的热度。

---

## 五、系统可靠性相关（Q51-Q60）

**Q51: 为什么选择最终一致性而不用Seata两阶段提交？**

两阶段提交虽然能保证强一致性，但是性能特别差。第一阶段需要锁定所有资源等待所有参与者的投票，这个过程中资源被长期占用，吞吐量很低。而且一旦协调者宕机，整个系统就会hang住，没办法恢复。最终一致性虽然不是强一致的，但系统吞吐量高、容错能力强。对于支付这类场景，用户可以接受几秒钟的延迟，所以最终一致性是更好的选择。

**Q52: 最终一致性的核心是什么？什么时候才算"最终一致"？**

最终一致性的核心是：系统某个时刻可能不一致，但通过补偿机制保证最后一定会达到一致。在我们的系统里，支付成功后立即返回，消息发送给任务服务。如果任务服务当时宕机了，消息会缓存在RabbitMQ里。只要任务服务恢复，就会继续处理消息，最终任务状态和支付状态还是会一致。什么时候才算最终一致呢？就是支付表和任务表里对应的订单状态一样了。

**Q53: 系统某个微服务宕机了，其他服务怎么处理？**

其他服务的请求会经过Feign客户端的重试和负载均衡逻辑。如果调用的实例宕机了，Feign会尝试其他健康的实例。如果一个服务的所有实例都宕机了，那就没办法了，只能返回错误给调用者。不过我们可以配置降级逻辑，比如任务服务请求飞手服务失败，就用本地缓存的飞手信息或者默认值，这样不至于完全不可用。

**Q54: 支付服务和任务服务之间用消息队列解耦，有什么好处和坏处？**

好处是显而易见的：支付服务和任务服务可以独立扩展，一个服务故障不影响另一个服务，系统的可用性和吞吐量都提升了。坏处是系统变得复杂了，引入了消息队列这个新的中间件，需要处理消息丢失、重复消费这些问题。另外数据的一致性也变成了最终一致，不是强一致的。所以这是一个在可用性和一致性之间的权衡。

**Q55: 如果消息队列宕机，支付记录会丢失吗？为什么？**

支付记录本身不会丢失，因为支付成功后我们已经把记录保存到数据库了，这个是独立的。消息队列宕机只会影响任务服务无法及时收到通知，没办法及时创建任务。但当消息队列恢复后，支付服务会重新发送消息，任务服务继续处理，最终还是会一致。所以消息队列宕机不会导致数据丢失，只是会有一定时间的延迟。

**Q56: 系统的限流策略是怎样的？在哪些地方实现？**

我们用了两层限流。第一层在网关层，用令牌桶算法限制每秒的请求数，比如最多1000个请求。第二层在具体的服务里，使用Guava的RateLimiter限制某些关键接口的流量。比如支付接口，可能设置最多100个并发请求。超过限流阈值的请求会被拒绝，网关返回429错误码告诉客户端"服务繁忙请稍后重试"，客户端可以实现指数退避重试。

**Q57: 某个接口突然流量很大，系统怎么应对？**

首先网关层的限流会拦截一部分请求，防止过多请求进入系统。其次，如果某个服务的响应时间变长了，Feign的超时机制会触发，然后配置的降级逻辑就会启动，返回一个cached的响应或者缺省值。再次，如果某个服务响应错误率太高，熔断器就会打开，阻止继续调用这个服务，保护后端。所以整个系统有多层的保护机制。

**Q58: 如果任务服务响应很慢，会影响支付流程吗？为什么？**

不会影响。因为我们用了异步消息解耦，支付成功后支付服务直接返回给用户，不用等待任务服务处理完。任务服务再慢也不影响支付流程。这就是用消息队列解耦的最大好处。

**Q59: 系统的熔断和降级机制是怎样的？**

我们用Hystrix框架实现熔断和降级。熔断器有三个状态：闭合、开启、半开。正常情况下是闭合状态，请求可以正常通过。当某个服务的错误率在一段时间内超过阈值（比如50%）或者超时次数过多，熔断器就会打开，这时所有请求都会被拒绝，直接返回降级的响应。打开一段时间后（比如30秒），熔断器会进入半开状态，允许一部分请求通过进行探测，如果这些请求都成功了就关闭熔断器恢复正常，如果还是失败就继续打开。

**Q60: 监控告警系统怎么设置？什么情况下会触发告警？**

我们收集每个服务的日志和性能指标，上传到ELK或者其他监控系统。设置了几个关键的告警阈值：比如支付成功率低于99.9%就告警，接单率低于95%就告警，API响应时间超过500ms就告警，消息队列堆积超过1000条就告警。告警会通过邮件、短信、钉钉这些方式通知相关人员，让我们能及时发现问题。

---

## 六、并发测试与性能验证（补充问题）

**Q61: 1000+的并发是怎么测试的？用的什么工具？**

我们用JMeter进行压力测试。创建了一个测试脚本，模拟1000个虚拟用户同时发起任务分配请求。具体配置是：线程组设置1000个线程，ramp-up时间设置为60秒，也就是在60秒内平均分布这1000个请求。然后设置循环次数为10，这样总共会发送10000个请求。JMeter会记录每个请求的响应时间、成功率、吞吐量等指标。

**Q62: 在测试中发现了什么问题？是怎么优化的？**

测试的时候我们发现了几个问题。第一，数据库连接池不够，当并发达到500左右时就开始出现"获取连接超时"的错误。我们把Hikari连接池的最大连接数从20改到50。第二，Redis的连接也成了瓶颈，改成了连接池模式。第三，发现有些查询特别慢，加了索引之后响应时间明显下降。还有就是发现消息队列在并发高的时候消息堆积速度很快，我们增加了消费者的数量。

**Q63: 压力测试的结果是什么样的？怎么判断系统能支撑1000+并发？**

压力测试的关键指标是吞吐量（Throughput）和响应时间。最后的测试结果是：在1000并发的情况下，系统的吞吐量达到了3000-4000个请求每秒，平均响应时间在200ms左右，99percentile的响应时间在500ms以内。成功率维持在99.9%以上。我们认为如果这些指标都满足，系统就能支撑1000+并发。

**Q64: 压力测试的时候是真实数据还是虚拟数据？**

主要是虚拟数据。我们在测试数据库里构造了几千条飞手信息、任务数据、支付记录等，然后在这基础上进行压测。这样做是为了避免对生产数据的影响，而且虚拟数据可以随意构造成各种场景。不过后期我们也做过灰度测试，用小部分真实流量来验证系统的实际表现。

**Q65: 如果真实并发超过了压力测试的1000+，系统会怎样降级？**

如果并发真的超过了1000+，首先网关的限流会启动，把多余的请求拒绝。这样可以保证进入系统的请求不会太多。然后，对于一些不太重要的操作，比如记录日志、更新统计数据这些，我们会进行降级处理，直接跳过不执行。另外缓存的TTL会被缩短，减少数据库的压力。最坏的情况下，我们还可以切换到只读模式，停止接收新的任务，只处理已有的任务。

**Q66: 压力测试中最容易出现的问题是什么？**

最容易出现的问题就是数据库和缓存的连接耗尽。压测的时候我们会看到大量的"获取连接超时"或者"Redis连接池已满"这样的错误。另外就是内存溢出，如果缓存设置得不当会导致内存持续增长。还有一个比较隐蔽的问题是GC停顿，当JVM进行Full GC的时候，整个系统会停顿几秒钟，这个时候响应时间会特别长。所以我们需要调整JVM参数，选择合适的垃圾回收器。

**Q67: 线上突然流量暴增怎么应对？有没有做过灰度测试？**

灰度测试我们做过。就是先在线上给10%的用户切到新版本，观察数据有没有异常。如果一切正常就扩大到20%、50%、最后100%。如果突然流量暴增，首先会触发告警，我们的值班人员会立即看到。然后我们会检查各个服务的实时指标，如果发现某个服务响应时间变长或者错误率升高，就会启动应急预案。可能的措施包括：降级某些功能、扩容更多实例、启动限流、甚至临时调整业务逻辑。

**Q68: 单台服务器最多能支撑多少并发？怎么估算的？**

这个取决于服务器的配置和应用的实现。一般来说，一台8核16GB内存的服务器，跑着Spring Boot应用，使用Tomcat默认配置（最多200个线程），大概能支撑500-1000的并发。我们的估算方法是根据响应时间反推：假设平均响应时间是200ms，那么一个线程每秒最多能处理5个请求，200个线程就是1000个请求每秒。所以单台大概支持1000并发。如果要支持更高的并发，就需要水平扩展，加更多服务器。

**Q69: 有没有测试过单点故障时系统的表现？**

有的。我们模拟过各种故障场景：停掉某个微服务、让数据库宕机、关闭Redis、中断网络连接等。测试结果是：如果某个非核心服务故障，系统通过降级逻辑仍然可以继续运行。如果数据库故障，写操作会失败，但读操作可以从缓存里返回。如果Redis故障，系统响应时间会变长但仍然可用。最坏的情况就是网络中断，这时系统完全不可用，只能等网络恢复。

**Q70: 如果并发测试发现性能不达预期，一般有哪些优化方向？**

一般的优化方向有四个。第一是增加硬件资源，最直接但成本最高。第二是优化代码和算法，比如减少不必要的遍历、改进数据结构。第三是加缓存和索引，减少数据库查询。第四是改架构，比如用消息队列解耦服务、用分布式缓存代替本地缓存。我们通常是先尝试后三种，只有都尝试过了效果还不理想，才考虑加硬件。
