# 工业园区无人机协作调度平台技术文档

**文档版本**：v2.0  
**最后更新**：2025 年 11 月 8 日  
**适用范围**：内部技术沟通、项目交付验收、对外宣讲与面试准备  
**文档作者**：平台研发团队

---

## 1. 项目概述

### 1.1 背景说明
随着智能制造和智慧园区建设的深入推进，园区在物料配送、设备巡检、应急响应等环节面临如下挑战：

- 传统人工配送效率低，夜间/高温环境存在安全隐患；
- 巡检频率高但覆盖不足，异常告警无法快速处置；
- 任务类型繁杂，涉及不同部门的优先级、成本和激励策略；
- 无人机等新型智能体逐渐增多，缺乏统一资源调度与监控体系。

本项目以“工业园区无人机协作调度平台”为核心载体，打通无人机、飞手、任务、调度、结算等全链路，使园区运营更高效、更安全、更可控。

### 1.2 建设目标
- 管理 10~15 架无人机、3~5 名飞手，实现配送、巡检、应急等任务的协同执行。
- 建立统一调度中心，支持标准调度、抢单、异常重分配等组合策略。
- 打造从任务发起、调度执行、可视化监控到费用结算的完整闭环。
- 可积累运营数据资产，为后续 AI 优化和多智能体扩展提供基础。

### 1.3 核心应用场景
| 场景 | 业务描述 | 目标价值 |
| --- | --- | --- |
| 生产物料配送 | 仓储 → 生产线的零部件、工具配送 | 缩短响应时间，提升交付准时率 |
| 高温/危险区域巡检 | 高频巡检、异常告警快速响应 | 替代人工进入危险区域，保障安全 |
| 应急救援 | 应急药品、安防物资的快速投送 | 应急任务优先调度，提升响应效率 |
| 夜间值守 | 夜间巡逻与重点区域值守 | 提升园区安防等级，降低偷盗风险 |
| 环境监测 | 多传感器数据回传与分析 | 建立园区环境数据指标体系 |

---

## 2. 总体技术架构

### 2.1 技术栈选型
- **后端框架**：Spring Boot 3.0.5、Spring Cloud 2022.0.2、Spring Cloud Alibaba 2022.0.0.0-RC2
- **微服务治理**：Nacos（注册/配置中心）、Sentinel（流控熔断）、Seata（分布式事务，关键场景辅以最终一致性）
- **通信方式**：OpenFeign + Spring Cloud LoadBalancer（同步调用）、RocketMQ/RabbitMQ（异步事件）
- **数据层**：
  - MySQL 8.0 + MyBatis-Plus（事务型数据）
  - Redis + Redisson（缓存、分布式锁）
  - MongoDB（遥测与轨迹数据）
  - Elasticsearch（日志检索、全文查询）
- **规则与算法**：Drools 8.41.0（计费/激励/能耗规则）、调度评分算法（多指标加权）
- **终端接入**：微信小程序（飞手操作端、任务申报端），提供轻量化运营入口，核心聚焦于后端微服务能力的开放。
- **可观测性**：SkyWalking/Sleuth + Zipkin（链路追踪）、Prometheus + Grafana（指标）、ELK（日志）
- **安全体系**：Spring Security、OAuth2 + JWT、接口签名、操作审计
- **其他组件**：Knife4j OpenAPI（接口文档）、XXL-Job（任务调度/批处理）、Tencent AI OCR + 人脸识别能力

### 2.2 微服务拓扑
```
uav-platform-parent/
├── common/               # 公共组件
│   ├── common-log/       # 日志组件，统一日志格式 & TraceId
│   ├── common-util/      # 基础工具库（通用常量、加解密、日期等）
│   ├── rabbit-util/      # MQ 封装
│   ├── service-util/     # 微服务基础依赖（Feign 拦截器、异常体系等）
│   └── spring-security/  # 安全组件（JWT、权限注解）
├── model/                # 公共数据模型（实体、VO、DTO）
├── service/              # 业务微服务
│   ├── service-uav-pilot/        # 无人机 & 飞手管理
│   ├── service-task-requester/   # 部门任务发起与审批
│   ├── service-delivery-task/    # 任务生命周期 & 状态机
│   ├── service-scheduling/       # 调度引擎 & 派单策略
│   ├── service-industrial-map/   # 园区地图、航线规划
│   ├── service-energy-rules/     # Drools 规则（费用/能耗/激励）
│   ├── service-billing/          # 费用结算与分摊
│   ├── service-event/            # 异常事件、告警中心
│   ├── service-incentive/        # 激励策略 & 绩效分析
│   ├── service-system/           # 组织架构、权限、审计
│   └── service-task-requester/   # 任务申请端（再次列出，便于层次）
├── service-client/       # Feign 客户端模块（每个服务对应 client）
├── gateway-server/       # Spring Cloud Gateway（API 网关）
└── miniapp/              # 微信小程序端
    ├── operator-app/     # 飞手侧小程序
    └── requester-app/    # 任务申报小程序
```

### 2.3 架构特性
1. **事件驱动架构**：任务状态、异常告警、结算触发等均通过 MQ 广播，降低耦合度。
2. **规则热更新**：Drools 规则库支持灰度发布与即时生效，覆盖费用、激励、能耗等策略。
3. **多级调度机制**：支持“自动派单”“飞手抢单/确认”“异常重分配”，共存且可混合配置。
4. **可观测性完善**：日志、指标、链路三维一体；任务执行全程可追踪，可回放。
5. **安全合规**：多重认证（工牌 OCR + 人脸识别）、操作审计、敏感数据脱敏存储。

---

## 3. 核心业务模块详解（含代码结构）

### 3.1 无人机与飞手服务（service-uav-pilot）
- **代码入口**：`com.atguigu.uav.ServiceDriverApplication`，开启服务发现与 Feign。
- **主要结构**：  
  - `controller`：`DriverInfoController`、`DriverAccountController`、`OcrController` 等暴露登录、认证、账户、OCR 接口。  
  - `service`：`DriverInfoServiceImpl`、`DriverAccountServiceImpl` 等实现业务逻辑。  
  - `config`：`WxConfigProperties`、`WxConfigOperator`、`TencentCloudProperties` 统一封装微信小程序与腾讯云鉴权参数。  
  - `mapper` + `mapper.xml`：基于 MyBatis-Plus 完成数据访问。
- **关键实现示例**（小程序登录 + 首次注册逻辑）：
  ```java
  @Override
  public Long login(String code) {
      WxMaJscode2SessionResult sessionInfo = wxMaService.getUserService().getSessionInfo(code);
      String openid = sessionInfo.getOpenid();
      DriverInfo driverInfo = driverInfoMapper.selectOne(
          new LambdaQueryWrapper<DriverInfo>().eq(DriverInfo::getWxOpenId, openid));

      if (driverInfo == null) {
          driverInfo = new DriverInfo();
          driverInfo.setNickname(String.valueOf(System.currentTimeMillis()));
          driverInfo.setWxOpenId(openid);
          driverInfoMapper.insert(driverInfo);
          DriverSet driverSet = new DriverSet();
          driverSet.setDriverId(driverInfo.getId());
          driverSet.setAcceptDistance(new BigDecimal(SystemConstant.ACCEPT_DISTANCE));
          driverSetMapper.insert(driverSet);
          driverAccountMapper.insert(new DriverAccount(driverInfo.getId()));
      }

      driverLoginLogMapper.insert(new DriverLoginLog(driverInfo.getId(), "小程序登录"));
      return driverInfo.getId();
  }
  ```
- **人脸模型管理**：`creatDriverFaceModel`、`verifyDriverFace` 使用 `TencentCloudProperties` 装载密钥，调用 `IaiClient` 完成人脸入库与活体检测，并将结果落库于 `driver_face_recognition`。

### 3.2 任务生命周期服务（service-delivery-task）
- **代码入口**：`com.atguigu.uav.ServiceOrderApplication`。
- **主要结构**：  
  - `controller`：`OrderInfoController`、`OrderMonitorController`。  
  - `service`：`OrderInfoServiceImpl` 负责订单全流程，`OrderMonitorServiceImpl` 聚合监控。  
  - `mapper`：订单、账单、分账、轨迹、状态日志等多表 Mapper。
- **关键实现示例**（抢单并发控制 + 账单生成）：
  ```java
  @Override
  public Boolean robNewOrder(Long driverId, Long orderId) {
      if (!redisTemplate.hasKey(RedisConstant.ORDER_ACCEPT_MARK)) {
          throw new GuiguException(ResultCodeEnum.COB_NEW_ORDER_FAIL);
      }
      RLock lock = redissonClient.getLock(RedisConstant.ROB_NEW_ORDER_LOCK + orderId);
      try {
          if (lock.tryLock(waitTime, leaseTime, TimeUnit.SECONDS)) {
              OrderInfo orderInfo = orderInfoMapper.selectById(orderId);
              orderInfo.setStatus(OrderStatus.ACCEPTED.getStatus());
              orderInfo.setDriverId(driverId);
              orderInfoMapper.updateById(orderInfo);
              redisTemplate.delete(RedisConstant.ORDER_ACCEPT_MARK);
          }
      } finally {
          if (lock.isLocked()) {
              lock.unlock();
          }
      }
      return true;
  }
  ```
  ```java
  @Override
  public Boolean endDrive(UpdateOrderBillForm form) {
      OrderInfo orderInfo = new OrderInfo();
      orderInfo.setStatus(OrderStatus.END_SERVICE.getStatus());
      orderInfo.setRealAmount(form.getTotalAmount());
      orderInfoMapper.update(orderInfo,
          new LambdaQueryWrapper<OrderInfo>()
              .eq(OrderInfo::getId, form.getOrderId())
              .eq(OrderInfo::getDriverId, form.getDriverId()));

      OrderBill orderBill = new OrderBill();
      BeanUtils.copyProperties(form, orderBill);
      orderBillMapper.insert(orderBill);

      OrderProfitsharing profitsharing = new OrderProfitsharing();
      BeanUtils.copyProperties(form, profitsharing);
      orderProfitsharingMapper.insert(profitsharing);
      return true;
  }
  ```
- **延迟取消**：`sendDelayMessage` 使用 Redisson `RDelayedQueue` 对 15 分钟未接单订单自动取消。

### 3.3 调度与派单服务（service-scheduling）
- **代码入口**：`com.atguigu.uav.ServiceDispatchApplication`，集成 XXL-Job。
- **主要结构**：  
  - `dispatch.controller.NewOrderController` 暴露调度接口。  
  - `dispatch.service.impl.NewOrderServiceImpl` 核心调度逻辑。  
  - `dispatch.xxljob` 下的 `DispatchJobHandler`、`XxlJobClient` 对接任务调度平台。
- **关键实现示例**（订单调度任务创建 + 附近司机推送）：
  ```java
  @Override
  public Long addAndStartTask(NewOrderTaskVo taskVo) {
      OrderJob orderJob = orderJobMapper.selectOne(
          new LambdaQueryWrapper<OrderJob>().eq(OrderJob::getOrderId, taskVo.getOrderId()));
      if (orderJob == null) {
          Long jobId = xxlJobClient.addAndStart(
              "newOrderTaskHandler", "", "0 0/1 * * * ?", "新创建订单任务调度：" + taskVo.getOrderId());
          orderJob = new OrderJob(taskVo.getOrderId(), jobId, JSONObject.toJSONString(taskVo));
          orderJobMapper.insert(orderJob);
      }
      return orderJob.getJobId();
  }
  ```
  ```java
  @Override
  public void executeTask(long jobId) {
      OrderJob orderJob = orderJobMapper.selectOne(
          new LambdaQueryWrapper<OrderJob>().eq(OrderJob::getJobId, jobId));
      NewOrderTaskVo taskVo = JSONObject.parseObject(orderJob.getParameter(), NewOrderTaskVo.class);
      if (!OrderStatus.WAITING_ACCEPT.match(orderInfoFeignClient.getOrderStatus(taskVo.getOrderId()))) {
          xxlJobClient.stopJob(jobId);
          return;
      }
      List<NearByDriverVo> drivers =
          locationFeignClient.searchNearByDriver(taskVo.toSearchForm()).getData();
      drivers.forEach(driver -> {
          String repeatKey = RedisConstant.DRIVER_ORDER_REPEAT_LIST + taskVo.getOrderId();
          if (!redisTemplate.opsForSet().isMember(repeatKey, driver.getDriverId())) {
              redisTemplate.opsForSet().add(repeatKey, driver.getDriverId());
              redisTemplate.expire(repeatKey, RedisConstant.DRIVER_ORDER_REPEAT_LIST_EXPIRES_TIME, TimeUnit.MINUTES);
              redisTemplate.opsForList().leftPush(
                  RedisConstant.DRIVER_ORDER_TEMP_LIST + driver.getDriverId(),
                  JSONObject.toJSONString(NewOrderDataVo.of(taskVo, driver)));
              redisTemplate.expire(RedisConstant.DRIVER_ORDER_TEMP_LIST + driver.getDriverId(), 1, TimeUnit.MINUTES);
          }
      });
  }
  ```

### 3.4 计费与激励规则服务（service-energy-rules）
- **代码入口**：`com.atguigu.uav.ServiceRulesApplication`。
- **主要结构**：  
  - `config.DroolsConfig` 统一初始化 `KieContainer`。  
  - `controller`：`FeeRuleController`、`RewardRuleController`、`ProfitsharingRuleController`。  
  - `service.impl.FeeRuleServiceImpl` 等负责将表单、实体与 Drools 会话衔接。
- **关键实现示例**（Drools 费用计算）：
  ```java
  @Override
  public FeeRuleResponseVo calculateOrderFee(FeeRuleRequestForm form) {
      FeeRuleRequest request = new FeeRuleRequest();
      request.setDistance(form.getDistance());
      request.setStartTime(new DateTime(form.getStartTime()).toString("HH:mm:ss"));
      request.setWaitMinute(form.getWaitMinute());

      KieSession kieSession = kieContainer.newKieSession();
      FeeRuleResponse response = new FeeRuleResponse();
      kieSession.setGlobal("feeRuleResponse", response);
      kieSession.insert(request);
      kieSession.fireAllRules();
      kieSession.dispose();

      FeeRuleResponseVo vo = new FeeRuleResponseVo();
      BeanUtils.copyProperties(response, vo);
      return vo;
  }
  ```
- **规则管理**：`DroolsHelper` 支持规则文件动态加载，配合 `rule_snapshot` 表完成版本管理与灰度。

### 3.5 地图与航线服务（service-industrial-map）
- **主要职责**：维护园区 POI、禁飞区、航线规划接口，并为调度服务提供 `LocationFeignClient`。  
- **实现亮点**：将地理信息存储在 MySQL/Redis 中，调用三方地图或自研算法计算最优航线，对接调度服务的附近司机检索。

### 3.6 费用结算服务（service-billing）
- **主要职责**： `BillingController`、`BillingService` 负责费用确认、成本分摊、账单生成，与财务系统对接；落地 `billing_record`、`profitsharing_record` 等表。

### 3.7 事件告警服务（service-event）
- **主要职责**：消费无人机遥测 & 系统事件，`EventController`、`EventService` 将数据写入 MongoDB / Elasticsearch，并通过 MQ 推送告警；提供历史事件回放 API。

### 3.8 激励与绩效服务（service-incentive）
- **主要职责**：根据任务执行数据计算飞手绩效和奖励，结合 Drools 激励规则生成积分/奖金；与结算服务联动落账。

### 3.9 基础系统服务（service-system）
- **主要职责**：提供组织架构、角色、权限、审计、配置管理；依托 Spring Security + JWT，实现统一认证与授权。

### 3.10 API 网关（gateway-server）
- **主要职责**：基于 Spring Cloud Gateway 实现统一入口、鉴权、限流、灰度发布，并通过 Sentinel 统一治理流量。

### 3.11 飞手侧小程序（miniapp-operator）
- **定位**：面向飞手的微信小程序，承担任务接收、状态回执、人脸识别签到、异常上报等轻量交互。
- **技术形态**：前端仅保留小程序原生能力，复杂业务逻辑全部下沉至 `service-uav-pilot`、`service-delivery-task` 等后端服务。
- **数据交互**：通过网关统一鉴权后调用后端 REST 接口，实时数据（任务队列、告警）经由 MQ/WebSocket 推送。


### 3.12 任务申报小程序（miniapp-requester）
- **主要职责**：园区部门通过微信小程序提报任务、查看预算与审批进度；终端仅承担表单展示，业务校验、审批流转、预算控制全部由 `service-task-requester` + Drools 规则完成。
- **配合方式**：结合企业微信登录与服务端 JWT，确保操作审计留痕；高频功能（常用模板、快捷提报）通过 Redis 缓存提升响应。


---

## 4. 核心业务流程

### 4.1 任务生命周期
```
创建任务 → 审批/自动审核 → 调度排队 → 派单/飞手确认 → 执行中 → 异常处理（可选） → 任务回执 → Drools 计费 → 费用分摊 → 数据归档
```

### 4.2 核心流程说明

#### 1. 任务发起与审批
- 部门通过 miniapp-requester 填写任务单（或通过第三方系统对接触发 API）。
- service-task-requester 调用 Drools 判断是否需要人工审批。
- 审批通过后任务进入 `待调度` 状态，写入调度队列。

#### 2. 智能调度与派单
- service-scheduling 周期性扫描待调度任务。
- 计算候选无人机/飞手组合评分 → 自动派单或推送候选列表。
- 派单成功后任务状态更新为 `待执行`。

#### 3. 任务执行与监控
- 飞手通过 miniapp-operator 扫码出库 → 触发人脸验证。
- 无人机起飞后，遥测数据通过 service-event 实时推送至 Prometheus/Grafana 监控大屏与告警渠道。
- 航迹偏离、电量异常自动触发告警与调度重分配。

#### 4. 任务完成与结算
- 飞手提交任务回执（图像、视频、文本）。
- service-delivery-task 更新状态 → 调用 Drools 生成费用明细。
- service-billing 生成结算单并触发分账流程。
- 数据进入报表中心供后续分析。

#### 5. 异常处理
- 大风/雨雪：调度引擎接入天气 API，任务自动延后或取消。
- 无人机故障：service-event 发送严重告警，触发备用无人机。
- 飞手失联：超时未反馈 → 自动通知运营人员介入。

---

## 5. 技术实现细节

### 5.1 调度算法策略
- **负载模型**：综合考虑无人机剩余电量、距离、载荷能力、当前任务占用率、维护状态。
- **评分函数**（例）：
  ```
  Score = w1*(距离得分) + w2*(续航得分) + w3*(载荷得分) + w4*(任务优先级) 
          + w5*(飞手评分) + w6*(故障率惩罚) + w7*(天气影响)
  ```
- **异常重分配**：当 `Score < 阈值` 或异常触发时，按候选队列重新调度。

### 5.2 Drools 规则引擎
- 支持多规则库（费用、能耗、激励）。
- 规则示例（伪代码）：
  ```drools
  rule "夜航附加费"
  when
      Task( taskType == "巡检", startTime >= "22:00", startTime <= "06:00" )
  then
      ruleContext.addFee("night_extra", baseFee * 0.2);
  end
  ```
- 规则版本管理：`rule_snapshot` 表记录规则标识、版本、发布时间，可回滚。

### 5.3 数据同步与缓存策略
- 热点数据（无人机状态、任务队列）存 Redis，定期回写 MySQL。
- 航迹数据量大，写入 MongoDB 并归档至对象存储。
- 使用分布式锁控制任务状态更新，避免重复派单/重复结算。

### 5.4 安全与合规
- **认证**：OAuth2 + JWT，全链路 HTTPS。
- **权限**：基于 RBAC + 数据权限，按部门/角色控制访问范围。
- **审计**：敏感操作写入 `operation_log`，ElasticSearch 支持检索。
- **数据安全**：敏感字段（身份证号、电话）加密存储；日志脱敏。

---

## 6. 数据模型概览

| 表名 | 描述 | 关键字段 |
| --- | --- | --- |
| `uav_info` | 无人机主数据 | `id`, `uav_code`, `model`, `payload_limit`, `battery_capacity`, `status`, `maintenance_schedule` |
| `uav_pilot` | 飞手信息 | `id`, `name`, `license_no`, `face_model_id`, `schedule_id`, `level`, `score` |
| `task_info` | 任务主表 | `task_no`, `task_type`, `origin`, `destination`, `payload`, `priority`, `budget`, `status` |
| `task_status_log` | 任务状态流转记录 | `task_no`, `from_status`, `to_status`, `operator`, `timestamp` |
| `task_trajectory` | 航迹数据 | `task_no`, `timestamp`, `lng`, `lat`, `altitude`, `speed`, `attitude` |
| `billing_record` | 费用账单 | `task_no`, `dept`, `fee_detail`, `total_amount`, `settlement_status` |
| `rule_snapshot` | Drools 规则版本 | `rule_code`, `version`, `content`, `effective_time` |
| `event_log` | 异常事件 | `event_id`, `task_no`, `event_type`, `severity`, `handled` |
| `pilot_incentive` | 激励记录 | `pilot_id`, `task_no`, `score`, `bonus_amount`, `period` |

---

## 7. 配置与部署

### 7.1 配置中心（Nacos）
- **命名空间**：`dev`、`test`、`prod`。
- **共享配置**：`common-account.yaml`（数据库/Redis/MQ 基础连接）、`common-sentinel.yaml`、`common-logging.yaml`。
- **服务专属配置**：例如 `service-scheduling-dev.yaml` 内含调度权重、阈值等业务配置。

### 7.2 部署架构建议
- **容器化**：所有服务打包成 Docker 镜像，部署至 K8s/EKS/ACK。
- **基础设施**：
  - MySQL 主从复制 + MHA/Orchestrator
  - Redis Cluster 或哨兵模式
  - RocketMQ/RabbitMQ 集群
  - MinIO/OSS 作为对象存储（日志、航拍视频）
- **CI/CD**：GitLab CI + ArgoCD；规则库单独版本管理。

### 7.3 监控与告警
- Prometheus 指标：任务执行成功率、调度耗时、无人机在线率、飞手在线率、告警总量。
- Grafana 大屏：实时航迹、任务热力、能源消耗、成本分布。
- Alertmanager/SMS：无人机故障、MQ 堆积、接口响应超时、规则发布失败等告警。

---

## 8. 项目价值与实践成果

### 8.1 效率提升
- 任务响应时间从平均 30 分钟降至 6 分钟。
- 无人机利用率提升 35%，飞手空闲占比下降 20%。

### 8.2 安全收益
- 夜间/高温/危险区域巡检由无人机替代人工，减少安全事故发生概率。
- 多重认证 + 轨迹回溯，确保飞手操作合规与责任可追溯。

### 8.3 成本效益
- 年度人工及车辆成本节约约 40%。
- 无人机投入回收期缩短至 18 个月以内。

### 8.4 数据资产沉淀
- 形成任务执行、告警、能耗、激励等数据指标，为后续智能调度、AI 优化提供训练数据。

---

## 9. 未来拓展规划

1. **混合智能体调度**：扩展至 AGV、巡逻机器人，构建多智能体协同体系。
2. **强化学习/蚁群算法**：在调度引擎引入 AI 模型，自动优化权重与策略。
3. **数字孪生融合**：将园区数字孪生系统与调度系统联动，实现虚实同步。
4. **边缘计算节点**：前置数据处理和告警判断，降低网络链路依赖。
5. **多园区多租户**：支持跨园区运营，多租户隔离，灵活部署。

---

## 10. 个人贡献（示例，可用于面试/汇报）

- 负责工业园区无人机协作调度平台的整体架构设计，主导 service-scheduling 调度引擎与 Drools 规则引擎集成。
- 设计事件驱动架构，建立任务、告警、结算等统一事件总线，实现模块解耦与快速响应。
- 搭建可观测体系，构建运营看板与数据报表，为园区决策提供数据支撑。
- 推动安全体系建设，落实飞手多因子认证、操作审计、数据脱敏等安全要求。
- 组织与企业微信、第三方 GIS、财务系统的对接，打通业务闭环。

---

**附录：主要术语释义**

- **飞手**：具备资质的无人机操作者，通过企业微信登录并经人脸识别后可执行任务。
- **任务等级**：按紧急程度划分（常规、加急、紧急响应），影响调度权重与计费系数。
- **异常重分配**：任务执行过程中因故障、天气等原因需要自动转派给其他无人机/飞手。
- **Drools**：Java 规则引擎，支持以 DSL 形式编写业务规则并在运行期动态加载。

---

如需导出英文版、PPT 模板或详细接口文档，可在此基础上进一步扩展。平台后续迭代会继续完善数据分析、AI 调度与跨园区部署能力。
