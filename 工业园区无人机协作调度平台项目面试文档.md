# 工业园区无人机协作调度平台项目面试文档 - 后端工程师

## 1. 项目概述类问题

### Q1: 请简单介绍一下你做的工业园区无人机协作调度平台
**回答要点：**
这是一个面向大型工业园区的无人机协作调度系统，聚焦物料配送、巡检、应急响应等场景。平台采用 Spring Cloud Alibaba 微服务架构，覆盖无人机与飞手管理、任务全生命周期、调度派单、Drools 计费规则、费用结算、告警监控等 10 余个核心服务，对外主要通过微信小程序（飞手操作端、任务申报端）提供能力。

**技术栈：**
- 后端：Spring Boot 3.0.5 + Spring Cloud 2022.0.2 + Spring Cloud Alibaba
- 数据：MySQL 8.0、Redis、MongoDB、Elasticsearch
- 消息：RocketMQ / RabbitMQ（视部署环境切换）
- 规则与算法：Drools、Redisson 分布式锁、XXL-Job 调度
- 第三方：腾讯云 OCR & 人脸识别、地图 / 气象数据服务
- 前端：微信小程序（飞手操作端、任务申报端）

**STAR 叙述模板：**
- **Situation（场景）**：园区无人机业务从试点走向规模化，任务量在新厂区上线后激增 3 倍，原有人工作业无法满足“分钟级响应 + 安全合规”要求。
- **Task（目标）**：搭建一套可支撑多园区、多任务类型的协作调度平台，要求 95% 任务 5 分钟内派单、异常任务自动预警、费用结算可审计。
- **Action（行动）**：
  1. 牵头梳理飞手、无人机、任务等核心领域模型，沿用代驾项目的微服务骨架快速拆分 `service-scheduling`、`service-energy-rules` 等服务。
  2. 设计调度评分引擎，利用 XXL-Job + Redis Sorted Set 做候选池；引入 Drools 统一计费/激励规则，结合 MQ 事件驱动保证最终一致性。
  3. 搭建执行监控链路，IOT 遥测接入 `service-event`，Redis Stream + MongoDB 存储航迹，Grafana 大屏实时展示；补齐人脸 + OCR + 审计的安全闭环。
- **Result（结果）**：平台上线后任务平均派单时长从 8 分钟降至 2.6 分钟，异常回收率提高 40%；Drools 规则热更新使运营配置效率提升 3 倍；同时通过审计与报表满足园区安全合规验收。

### Q2: 这个项目的业务流程是怎样的？
**回答要点：**
1. **任务申报**：园区部门在任务申报小程序填写需求，经由 `gateway-server` 路由至 `service-task-requester`。
   - 该服务调用 Drools 轻量规则判断预算、优先级与黑名单客户，合格的任务直接写入 `task_info` 并落 `task_status_log`。
   - 若触发人工审批，利用内置审批流（状态机 + Redis 延迟队列）通知运营后台，通过 RocketMQ 主题 `task.audit` 推送审核事件。
2. **智能调度**：`service-scheduling` 订阅 `task.created` 事件，结合 `service-industrial-map` 提供的禁飞区、路径长度与 `service-uav-pilot` 的无人机/飞手实时状态计算评分。
   - 调度核心在 XXL-Job 定时触发，利用 Redis Sorted Set 缓存候选列表，评分公式按距离、电量、载荷、优先级、故障率动态加权。
   - 满足阈值的任务直接调用 Feign 接口下发给飞手；阈值不足时将候选列表投递到 MQ，供运营或飞手小程序二次确认。
3. **执行监控**：飞手到库房扫码，`service-uav-pilot` 校验工牌 OCR、人脸模型（`driver_face_recognition`）后更新任务状态为待执行。
   - 执行过程中无人机遥测数据（航迹、电量、姿态、告警）通过 IOT 通道写入 `service-event`，先进入 Redis Stream，异步批量落 MongoDB `task_trajectory`。
   - 运营大屏通过 WebSocket 订阅 `service-event` 推送，实现秒级轨迹和告警展示。
4. **任务完成**：飞手在小程序提交回执，`service-delivery-task` 状态机推进至 `COMPLETED`，并发送 `task.completed` 事件。
   - `service-energy-rules` 调用 Drools 规则库（费用、能耗、激励）生成结算明细，写入 `billing_record`，同时推送 MQ 通知 `service-billing` 完成分账。
   - 汇总数据经 `service-report` 构建指标，写入 Elasticsearch 供报表查询。
5. **异常处理**：`service-event` 对天气、故障、电量阈值进行实时检测，命中则发布 `task.exception` 事件。
   - `service-scheduling` 根据异常类型执行任务召回或重分配，必要时降级为人工派单；异常闭环记录在 `event_log`，供稽核和回放使用。

## 2. 微服务架构类问题

### Q3: 为什么选择微服务架构？有什么优缺点？
**回答要点：**

**选择原因：**
- 业务域清晰：飞手管理、调度引擎、规则计算、监控告警各自独立。
- 团队协作：支持多团队并行迭代（算法组、规则组、运维组等）。
- 弹性扩缩：调度、任务、监控等高并发模块需要独立扩容。

**优点：**
- 服务可独立部署、隔离故障域。
- 技术选型灵活（调度与规则引擎可按需升级）。
- 服务拆分提升开发与迭代效率。

**缺点：**
- 分布式事务和一致性处理复杂。
  - 解决策略：关键链路结合 Seata、事件最终一致性。
- 运维成本高，需要完善的自动化发布与监控体系。

### Q4: 服务是如何拆分的？各自职责是什么？
**回答要点：**
- **service-uav-pilot**：无人机、飞手资质、OCR/人脸建模、飞行日志。
- **service-task-requester**：任务申报、预算校验、审批流。
- **service-delivery-task**：任务状态机、延迟取消、轨迹记录。
- **service-scheduling**：调度算法、候选派单、XXL-Job 批调度。
- **service-industrial-map**：园区地图、禁飞区、路径规划、附近资产查询。
- **service-energy-rules**：Drools 计费/激励规则执行。
- **service-billing**：费用结算、成本分摊、分账接口。
- **service-event**：实时告警、航迹存储、事件回放。
- **service-incentive**：飞手绩效、积分奖励、激励策略。
- **service-system**：组织、权限、审计、配置、登录。
- **gateway-server**：统一入口、鉴权、限流。

### Q5: 服务之间如何通信？如何保证可靠性？
**回答要点：**
- **同步通信**：OpenFeign + LoadBalancer，结合超时重试、熔断降级。
- **异步通信**：RocketMQ/RabbitMQ 推送任务事件、告警、结算通知，实现最终一致性。
- **可靠性措施**：
  - Feign 结合 Sentinel 做降级。
  - MQ 启用 confirm/return、消费 ACK、死信队列。
  - 对幂等操作（如结算）使用业务去重或分布式锁。

## 3. 数据库设计类问题

### Q6: 数据库是如何设计的？有哪些关键表？
**回答要点：**
- 按业务域拆分库：飞手、任务、结算、规则等各自独立。
- 核心表：
  - `uav_info`、`uav_pilot`：无人机与飞手主数据。
  - `task_info`、`task_status_log`、`task_trajectory`：任务主表、状态流水、航迹数据（大数据量部分落 MongoDB）。
  - `billing_record`、`profitsharing_record`：费用、分账记录。
  - `rule_snapshot`：Drools 规则版本管理。
  - `event_log`：告警与异常事件。
- 索引设计：订单号、任务号、飞手/无人机 ID 为主；航迹数据分库分表、时间分区。

### Q7: 如何保证分布式事务的一致性？
**回答要点：**
- **关键链路**（任务完成 → 费用结算 → 分账）：
  - 使用 Seata AT 事务或 TCC，结合事件驱动保证最终一致。
- **非关键链路**（告警推送、积分发放）：
  - 使用 MQ + 状态机 + 补偿表，实现幂等消费与失败重试。
- **具体策略**：
  - 订单状态、费用结算记录带版本号或状态位，防止重复更新。
  - 消息事务表记录发布状态，结合定时补偿任务。

## 4. 缓存与并发控制类问题

### Q8: 项目如何使用 Redis？
**回答要点：**
1. 调度候选列表（司机候选队列、任务队列、重复推送标识）。
2. 无人机/飞手实时状态缓存，实现秒级可视化。
3. 分布式锁：抢单、调度、结算幂等控制（Redisson）。
4. 延迟队列：订单超时取消、激励发放延迟生效。

### Q9: 如何处理缓存一致性与热点问题？
**回答要点：**
- Cache Aside 模式：更新 DB 后删除缓存，热点数据设定合理 TTL。
- 对关键计数使用 Redis 原子操作（如排班计数）。
- 高频航迹数据写入 Redis Stream，再异步落 MongoDB，避免写入压力。
- 引入限流保护，防止缓存穿透；同时采用布隆过滤器过滤非法请求。

## 5. 第三方服务集成类问题

### Q10: 平台如何集成腾讯云 OCR 与人脸识别？
**回答要点：**
- 在 `service-uav-pilot` 中封装 `TencentCloudProperties`，通过 `IaiClient` 创建人脸模型、校验、活体检测。
- OCR 识别执照/工牌，校验飞手资质。
- 人脸验证流程：  
  1. 登陆 / 执飞前人脸比对；2. 静态活体检测；3. 记录 `driver_face_recognition` 表防止重复验证。
- 安全注意：密钥通过 Nacos 配置中心注入，结合 KMS 做密钥管控。

### Q11: 地图与气象服务如何使用？
**回答要点：**
- 地图服务：维护 POI、禁飞区、航线；提供附近无人机查询 `searchNearByDriver` 接口。
- 气象服务：定时获取风速、降雨数据，调度前判断是否满足安全阈值；异常时触发告警与任务重分配。
- 接口调用封装在 `service-industrial-map`，对外提供 Feign Client，业务层只需调用 `LocationFeignClient`。

## 6. 性能优化类问题

### Q12: 如何应对高并发调度和任务推送？
**回答要点：**
- 调度服务异步化：调度任务由 XXL-Job 驱动，核心逻辑使用线程池 + 批量处理。
- Redis 去重 & 队列：任务推送前判断是否已推给某飞手，1 分钟内队列自动过期。
- 分布式锁控制抢单/接单竞争；落堆操作使用 Lua 脚本或乐观锁。
- 调度评分缓存：将部分评分参数（距离、续航等）缓存，降低实时计算压力。

### Q13: 数据查询性能如何优化？
**回答要点：**
- 航迹、告警等大数据采用 MongoDB + Elasticsearch，提供近实时查询。
- MySQL 表按时间或园区分片；热点表启用索引优化与 SQL 审核。
- 使用 MyBatis-Plus 分页查询结合缓存，避免全表扫描。
- 报表类查询异步化，预计算指标写入汇总表。

## 7. 规则引擎类问题

### Q14: 为什么选择 Drools？如何落地？
**回答要点：**
- 计费、激励、能耗补偿等规则变化频繁，需要可视化配置、热更新。
- Drools 支持规则与代码解耦，运营人员可以配置 YAML/DRL 后即时生效。
- 实现：`service-energy-rules` 中 `DroolsConfig` 初始化 `KieContainer`。请求进入时构建 `FeeRuleRequest`，设置 `feeRuleResponse` 全局变量执行规则，返回 `FeeRuleResponseVo`。
- 规则管理：`rule_snapshot` 表记录版本，发布流程支持灰度校验、回滚。

## 8. 系统设计类问题

### Q15: 任务状态机是如何设计的？
**回答要点：**
- 状态枚举：`WAITING_REVIEW`、`WAITING_DISPATCH`、`WAITING_CONFIRM`、`IN_PROGRESS`、`COMPLETED`、`ABORTED`、`SETTLED` 等。
- 流转规则：严格按照业务流程推进，结合数据库状态字段 + 日志表记录轨迹。
- 并发控制：状态更新时带版本号或状态判断；结合分布式锁防止重复完成。

### Q16: 调度算法如何兼顾效率与安全？
**回答要点：**
- 评分维度：距离、电量、载荷、任务等级、历史故障率、飞手评分、天气指数。
- 动态权重：高峰时段、应急任务提升优先级；夜间/恶劣天气降低派单阈值。
- 安全保障：若评分低于阈值或天气不满足条件，自动延迟或拒绝派单；异常时触发重分配。
- 调度策略可配置，支持自动派单与飞手确认双模式。

## 9. 监控运维类问题

### Q17: 如何监控微服务运行状况？
**回答要点：**
- Spring Boot Actuator + Prometheus 采集指标，Grafana 大屏展示任务成功率、调度耗时、告警数量。
- SkyWalking / Sleuth + Zipkin 做链路追踪，定位跨服务延迟问题。
- 日志统一输出 JSON，接入 ELK；关键操作日志（审批、调度、告警处理）做审计存档。

### Q18: 系统故障如何快速处理？
**回答要点：**
- Sentinel 限流降级，防止调度服务雪崩。
- MQ 堆积监控，超阈值触发自动扩容或告警。
- 关键服务支持灰度发布、快速回滚；配合蓝绿部署减少停机。
- 建立应急预案：任务人工接管、飞手手动退回、告警手动确认流程。

## 10. 安全性问题

### Q19: 系统如何保障安全？
**回答要点：**
- 认证授权：Spring Security + OAuth2 + JWT，多端登录，权限分级。
- 数据安全：敏感信息加密存储，日志脱敏，HTTPS 全链路加密。
- 操作审计：审批、调度、告警处理等关键操作写入审计表。
- 人机安全：飞手必须人脸识别通过后才能启飞；异常巡检需二次确认。

### Q20: 如何防止恶意调用或数据泄露？
**回答要点：**
- 接口签名 / 时间戳验证，防止重放攻击。
- Sentinel + 自定义注解实现接口限流、黑白名单。
- 配置中心与密钥使用加密库 / KMS 管理，敏感配置不落盘。
- 重要数据访问日志化，异常行为告警（如频繁导出、越权访问）。

## 11. 消息队列类问题

### Q21: 为什么选择 RocketMQ/RabbitMQ？如何保证消息可靠性？
**回答要点：**
- RocketMQ 擅长高吞吐延迟消息；RabbitMQ 在简易部署场景更便捷。平台支持两者切换以适配不同环境。
- **可靠性**：
  - 生产者开启 confirm/return，失败自动重试。
  - 消息持久化，队列设置死信交换机。
  - 消费端手动 ACK，出现异常 Nack + 重试或转入补偿队列。
  - 业务侧确保幂等性（以任务号、订单号为幂等键）。

### Q22: 如何避免消息重复消费？
**回答要点：**
- 幂等校验：消费前查询状态或使用 Redis set 记录处理过的消息 ID。
- 数据库层加唯一索引 / 状态机控制（如订单状态只能前进不能后退）。
- 分布式锁控制并发消费，确保单条消息只被一个实例处理成功。

## 12. 项目难点问题

### Q23: 项目中遇到的最大技术难点是什么？如何解决的？
**回答要点：**
**难点：调度过程中既要保证实时性，又要兼顾安全与可靠。**

- **问题**：新任务高峰期（例如夜间巡检集中）调度服务压力大，同时需要实时评估天气、禁飞区、电量等约束。
- **解决方案**：
  1. 引入 XXL-Job + Redis 批调度，将调度任务批处理。
  2. 周边无人机候选结果缓存，同时定期刷新，减少实时查询压力。
  3. 将天气、禁飞区数据做本地缓存 + 定时同步，确保调度时可快速判断。
  4. 调度结果通过 MQ 推送，任务确认与异常反馈异步处理，保证核心流程不被阻塞。

### Q24: 如何保证系统高可用？
**回答要点：**
- 服务层：多实例部署 + Sentinel 保护，网关熔断降级。
- 数据层：MySQL 主从切换、Redis 哨兵、MQ 集群、MongoDB 副本集。
- 拓扑层：关键服务部署在不同可用区，结合 K8s 自动恢复。
- 应用层：任务状态幂等处理、失败补偿、人工兜底流程（如调度故障时人工派单）。

## 13. 业务理解类问题

### Q25: 工业园区无人机协作的核心指标有哪些？如何优化？
**回答要点：**
- **核心指标**：任务响应时间、完单率、异常率、飞手在线率、无人机利用率、能耗成本。
- **优化策略**：
  - 优化调度权重，提高高优任务成功率。
  - 提升规则灵活性，针对夜航、应急任务设置不同奖励。
  - 加强运营大屏监控，及时发现瓶颈。

### Q26: 飞手激励体系如何设计？
**回答要点：**
- 绩效维度：准时率、完单率、夜航比例、异常处理响应。
- 激励方式：积分 / 现金奖励、等级晋升、班次优先权。
- 技术实现：Drools 激励规则 + 定期统计任务，通过 service-incentive 生成激励账单，与结算服务联动。

## 14. 扩展性问题

### Q27: 如果设备规模翻倍，系统如何扩展？
**回答要点：**
- 服务层横向扩容，调度服务拆分为 “常规调度” 与 “应急调度” 集群。
- 数据层引入分库分表、Read/Write 分离，MongoDB 根据航迹量扩展分片。
- 利用 K8s HPA + Prometheus 指标实现自动扩缩容。
- 调度算法支持多租户或多园区路由，减少单集群压力。

### Q28: 如何支持跨园区、多智能体调度？
**回答要点：**
- 增加园区维度：任务、无人机、规则都带 `park_id`，进行逻辑隔离。
- 引入多租户模式，配置中心和规则库支持按园区隔离。
- 调度引擎抽象为策略模式，可扩展至 AGV、机器人，实现混合调度。
- 大屏与报表按照园区分组展示，支持汇总与对比。

## 15. 学习与成长问题

### Q29: 通过这个项目你学到了什么？
**回答要点：**
- 微服务在复杂工业场景下的落地实践（调度、规则、事件驱动）。
- 与算法/运营协作，理解业务指标与技术指标的平衡。
- 人机安全、运维可观测性的重要性。
- 如何构建可扩展、可演进的业务规则体系。

### Q30: 如果重新设计这个系统，你会如何改进？
**回答要点：**
- 引入服务网格（Istio）简化东西向通信和可观测性。
- 调度算法引入机器学习/强化学习，提高自适应能力。
- 构建数字孪生，实现场景仿真验证调度策略。
- 完善自动化测试与回归，降低多规则上线风险。

---

## 面试建议

### 回答技巧
1. **STAR 法**：描述场景、任务、行动、结果，重点突出个人贡献。
2. **技术与业务结合**：不仅讲实现，还说明为什么这么做、解决了什么问题。
3. **指标驱动**：提到上线效果（效率、安全、成本优化）更具说服力。
4. **反思与优化**：适当说明遗留问题和改进方向，体现成长性。

### 准备要点
1. **熟悉全链路**：对任务申报、调度、执行、结算、异常闭环每一步的实现细节倒背如流。
2. **掌握关键代码**：重点准备调度评分、Drools 规则调用、MQ 事件监听、状态机流转等代码片段。
3. **准备指标数据**：掌握上线前后核心指标的变化（派单时长、异常回收率、规则自定义效率等）。
4. **梳理个人贡献**：明确自己负责的模块、解决的难点与产生的业务价值。

### 常见追问
- “调度失败或异常场景如何处理？”
- “Drools 规则冲突如何排查？”
- “跨园区部署需要考虑哪些安全问题？”
- “MQ 或 Redis 宕机时系统如何保证可用？”
- “如何量化项目带来的业务价值？”

充分准备以上问答点，并结合自己在项目中的实际负责内容进行补充，可有效提升面试中的专业度与说服力。记住：面试不仅是展示技术能力，更是展示解决问题的思路和持续学习的能力。
