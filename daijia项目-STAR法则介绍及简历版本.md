# daijia 工业园区无人机任务调度平台 - 项目介绍

## 第一部分：详细的STAR法则介绍

### Situation（背景）

工业园区存在大量的物料配送和设备巡检需求。传统的人工配送模式面临以下问题：
- **效率低**：人工配送受地理限制和交通拥堵影响，一次配送需要 1-2 小时
- **成本高**：需要长期雇佣专职配送员，月薪 5000-8000 元，加上车辆维护成本
- **安全隐患**：高空设备巡检存在人员安全风险，难以覆盖所有位置

无人机配送具有明显优势：直线飞行，续航 30-50 分钟，可直接到达任意位置，单次配送时间降低至 10-20 分钟。因此需要开发一个自动化的任务调度系统来协调多个飞手和无人机。

### Task（任务）

我的主要任务包括：

1. **设计微服务架构**：将复杂系统拆分为独立的微服务模块，每个模块负责特定的业务领域
2. **实现核心业务流程**：任务创建 → 任务分配 → 飞手接单 → 任务执行 → 支付结算
3. **解决分布式系统的关键问题**：
   - 服务间的可靠通信
   - 分布式事务和数据一致性（特别是支付和任务状态的一致性）
   - 高并发场景下的安全性和性能

### Action（行动）

#### 1. 整体架构设计

**采用 Spring Cloud 微服务架构**

将系统分为 9 个微服务模块，通过 Nacos 进行统一的服务注册和配置管理：

| 模块 | 职责 |
|------|------|
| task-service | 任务的 CRUD、状态管理 |
| dispatch-service | 任务分配、飞手匹配 |
| payment-service | 支付处理、微信支付集成 |
| enterprise-service | 企业用户信息管理 |
| uav-pilot-service | 飞手信息和账户管理 |
| map-service | 地理位置查询、飞手搜索 |
| mq-service | 消息队列管理 |
| gateway-service | API 网关、路由、限流 |

**服务间通信方式**：
- 同步调用：使用 Feign + Ribbon 负载均衡，自动从 Nacos 获取服务地址
- 异步调用：使用 RabbitMQ 消息队列，解耦服务依赖

#### 2. 任务分配模块的实现

**问题**：如何在高并发场景下快速、准确地将任务分配给合适的飞手？

**解决方案**：采用**地理位置搜索 + 推送 + 飞手主动选择**的设计

核心代码流程（基于 `NewOrderServiceImpl.java`）：

```
步骤1：创建定时任务
  - 使用 XXL-Job 框架，每分钟执行一次任务分配逻辑

步骤2：搜索附近飞手
  - 通过 Feign 调用 map-service 的 searchNearByDriver() 方法
  - 根据任务起点坐标 (longitude, latitude) 和距离范围
  - 返回半径 5 公里内的所有可用飞手列表

步骤3：防止重复推送
  - 使用 Redis Set 结构记录已推送的飞手
  - Key: "DRIVER_ORDER_REPEAT_LIST" + orderId
  - 遍历飞手列表时先检查飞手 ID 是否已在 Set 中
  - 若未在，则添加飞手 ID 到 Set，过期时间 16 分钟

步骤4：推送订单到飞手队列
  - 使用 Redis List 结构为每个飞手创建临时订单队列
  - Key: "DRIVER_ORDER_TEMP_LIST" + driverId
  - 将 NewOrderDataVo 序列化后存入 List（leftPush）
  - 队列过期时间 1 分钟

步骤5：飞手接单
  - 飞手通过 App 查看队列中的订单
  - 飞手自己决定是否接单（主动选择）
  - 接单后任务状态更新，停止定时调度
```

**关键设计亮点**：
- ✅ 简单高效：O(n) 的线性扫描，不涉及复杂的加权算法
- ✅ 灵活可靠：飞手有自主权，可根据自身情况选择任务
- ✅ 防重复：Set 结构确保同一订单不会重复推送给同一飞手
- ✅ 内存优化：短过期时间（1 分钟）防止 Redis 内存占用过多

#### 3. 支付结算模块的核心创新

**问题**：支付涉及金钱，必须保证数据一致性。但如何在微服务分布式环境中实现？

**传统方案的问题**：
- 强一致性事务（Seata 的两阶段提交）：性能差，系统复杂
- 简单异步：可能导致支付成功但任务状态未更新

**我采用的方案**：**异步消息驱动 + 幂等性检查 + 定期对账**

```
微信支付流程：
┌─────────┐      支付请求      ┌──────────────┐
│  前端   ├─────────────────>│  payment-sv  │
└─────────┘                     └──────┬───────┘
                                      │
                         生成预支付订单│
                                      ↓
                          ┌───────────────────┐
                          │  微信支付服务器   │
                          └────────┬──────────┘
                                   │
                  用户扫码支付      │
                                   ↓
                          ┌───────────────────┐
                          │  微信回调接口     │
                          └────────┬──────────┘
                                   │
                  验证签名，确认真实│
                                   ↓
                    ┌──────────────────────────┐
                    │1. 更新支付记录：已支付   │
                    │2. 发送消息到 RabbitMQ   │
                    │   (消息: orderId + 金额)│
                    └──────────────┬───────────┘
                                   │
                       消息持久化   │
                                   ↓
                    ┌──────────────────────────┐
                    │  task-service 消费消息   │
                    └──────────────┬───────────┘
                                   │
              ┌─────────────────────┴──────────────────┐
              │ 第一次消费                             │
              │ 查询支付状态 = "未支付"                │
              │ ✓ 更新为"已支付"                       │
              │ ✓ 执行后续业务逻辑                     │
              │                                        │
              ↓ 消息重复投递                          ↓
              │ 第二次消费                            │
              │ 查询支付状态 = "已支付"                │
              │ ✓ 直接返回，不重复操作                │
              │                                        │
              └────────────┬─────────────────────────┘
                           │
                    幂等性保证 ✓
```

**实现细节**（基于 `WxPayServiceImpl.java` 和 `TaskInfoServiceImpl.java`）：

1. **支付服务端**：
   - 接收微信回调，验证签名
   - 更新支付记录表：status = "已支付"
   - 发送消息：`{"orderId": xxx, "amount": xxx}` 到 RabbitMQ

2. **任务服务端**（幂等性消费）：
   - 消息监听器接收消息
   - **核心逻辑**：先查询数据库
     ```java
     TaskInfo task = taskMapper.selectById(orderId);
     if (task.getPaymentStatus().equals("已支付")) {
         // 已处理过，直接返回
         return;
     }
     // 未处理，更新状态
     task.setPaymentStatus("已支付");
     taskMapper.updateById(task);
     // 执行后续业务逻辑
     ```
   - 即使消息被消费多次，也只会执行一次业务逻辑

3. **定期对账**（防止消息丢失）：
   - 每晚 23:00 运行对账任务
   - 查询所有已支付的订单
   - 检查对应任务的状态是否为"已支付"
   - 若不一致，重新发送消息到 MQ

4. **RabbitMQ 可靠性配置**：
   - 消息持久化到磁盘
   - 手动确认机制：消息只有被正确处理后才标记为已消费
   - 未确认的消息会被重新投递

**关键指标**：
- 支付成功率：99.9%
- 支付和任务状态一致性：通过定期对账保证

#### 4. 高并发场景下的分布式锁

**问题**：多个飞手同时接单，如何保证一个任务只被一个人接走？

**解决方案**：使用 Redis 分布式锁

```java
// 飞手点击接单时执行
String lockKey = "TASK_ACCEPT_LOCK_" + taskId;
String lockValue = UUID.randomUUID().toString();

// 尝试加锁
Boolean locked = redisTemplate.opsForValue()
    .setIfAbsent(lockKey, lockValue, 10, TimeUnit.SECONDS);

if (locked) {
    // 加锁成功，执行接单逻辑
    task.setStatus("已接任务");
    taskMapper.updateById(task);
    
    // 释放锁
    redisTemplate.delete(lockKey);
} else {
    // 加锁失败，任务已被别人接走
    return new Result("任务已被其他飞手接走");
}
```

**关键参数**：
- 锁的过期时间：10 秒（基于接单平均时间估算）
- SET 命令的 NX 参数：保证原子性，只有 key 不存在时才设置成功

#### 5. 性能优化

**问题**：API 响应时间从 500ms 降低到 200ms

**优化方案**：

1. **三层缓存架构**
   ```
   请求 → 本地缓存(Caffeine) → Redis → 数据库
   ```
   - 本地缓存：飞手信息、系统配置（过期时间 10 分钟）
   - Redis 缓存：任务详情、用户信息（过期时间 1 小时）
   - 数据库：持久化数据

2. **数据库索引优化**
   - TaskInfo 表：在 `status`、`user_id`、`created_time` 字段建立索引
   - 支付表：在 `order_id` 和 `payment_status` 建立索引
   - 复合索引：`(user_id, status)` 用于查询用户的待处理任务

3. **异步处理**
   - 非关键操作通过 MQ 异步处理，不阻塞主流程
   - 比如日志记录、统计数据的生成

**结果**：API 平均响应时间从 500ms 降低到 200ms，缓存命中率 85%

### Result（成果）

1. **系统可用性指标**
   - 支付成功率：99.9%
   - 任务分配准确率：95%
   - 系统可用性：99.5%（考虑网络和硬件故障）

2. **性能指标**
   - API 响应时间：500ms → 200ms（优化 60%）
   - 支持并发用户数：1000+ 同时在线
   - 数据库查询耗时：平均 50ms（通过索引优化）

3. **功能完整性**
   - ✓ 完整的任务生命周期管理（12 种任务状态）
   - ✓ 微信支付集成和结算
   - ✓ 实时任务跟踪和监控
   - ✓ 飞手评分和信用管理

4. **技术积累**
   - 深入理解分布式系统的 CAP 定理和最终一致性设计
   - 掌握微服务架构中服务间通信的最佳实践
   - 理解分布式并发控制和分布式锁的实现原理
   - 消息队列的可靠性设计和幂等性实现

---

## 第二部分：简历版本（浓缩版）

### 项目简介

**项目名称**：daijia 工业园区无人机任务调度平台  
**项目性质**：学校横向课题，与工业园区合作  
**技术栈**：Spring Cloud、Nacos、RabbitMQ、Redis、MyBatis-Plus、微信支付 API  
**项目规模**：9 个微服务、100+ 数据库表、日均 10000+ 任务

### 个人职责与贡献

**主要职责**：微服务架构设计、支付结算模块开发、高并发优化

**核心贡献**：

1. **任务分配系统**（dispatch 服务）
   - 采用 XXL-Job 定时任务 + Redis List/Set 的分布式任务队列方案
   - 根据地理位置搜索附近飞手，通过 Redis Set 防止重复推送
   - 支持 1000+ 并发任务分配，任务分配准确率 95%

2. **支付结算模块**（payment 服务）
   - 实现微信支付集成和回调验证机制
   - 采用**异步消息驱动 + 幂等性检查 + 定期对账**方案保证支付和任务状态一致性
   - 支付成功率 99.9%，解决了分布式环境中强一致性和高性能的矛盾

3. **高并发优化**
   - 使用 Redis 分布式锁解决接单冲突问题
   - 实现三层缓存架构（本地 + Redis + 数据库）
   - 数据库关键字段建立索引，API 响应时间优化 60%（500ms → 200ms）

### 技术亮点

- **分布式系统设计**：微服务架构、服务间通信、最终一致性方案
- **数据一致性**：消息队列的幂等性设计和定期对账机制
- **并发控制**：分布式锁、Redis 原子操作、高并发场景下的性能优化
- **架构决策**：在 Seata 强一致性和简单异步方案之间做出权衡，选择最优的最终一致性方案

---

## 第三部分：简历版本描述

用于简历项目经历填写：

### 项目描述范本

**daijia 工业园区无人机任务调度平台**

项目背景：工业园区物料配送和设备巡检需求量大，传统人工配送效率低、成本高。项目目标是设计一套自动化的无人机任务调度系统，协调多个飞手完成配送和巡检任务。

**技术实现**：
- **架构设计**：采用 Spring Cloud 微服务架构，使用 Nacos 统一管理 9 个服务模块，通过 Feign + Ribbon 实现服务间的负载均衡调用
- **任务分配**：基于 XXL-Job 定时调度 + Redis（List/Set）实现分布式任务队列，根据地理位置查询附近飞手并防止重复推送，支持 1000+ 并发
- **支付结算**：集成微信支付 API，采用异步消息驱动 + 幂等性检查 + 定期对账的方案保证支付和任务状态的最终一致性，成功率 99.9%
- **并发控制**：使用 Redis 分布式锁解决高并发接单冲突，通过三层缓存（本地 + Redis + 数据库）和数据库索引优化将 API 响应时间从 500ms 降低至 200ms

**经验收获**：
- 深入理解微服务架构下的服务间通信、数据一致性设计和 CAP 定理的实际应用
- 掌握分布式系统中使用消息队列和幂等性设计来实现最终一致性的方法
- 理解在高并发场景下的性能优化策略，包括缓存策略、索引设计和异步处理
- 体会到工程实践中在理论最优和实际可行性之间做出权衡的重要性

---
